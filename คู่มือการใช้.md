# คู่มือการใช้ + ปุ่มลัดทั้งหมด

> วิธีติดตั้งโปรแกรมดูที่ [ติดตั้ง.md](ติดตั้ง.md) — หน้านี้คือวิธี "ใช้งาน"

## ขั้นตอนการทำงาน (ภาพรวม)

1. **เปิดไฟล์เสียง** (หรือไฟล์งาน `.audioedit.json` ที่เพื่อนส่งมา — ใช้ปุ่มเดียวกัน)
2. ได้ตัวหนังสือมา 2 ทาง: **ถอดเสียง** (ให้ AI ถอด) หรือ **ตรึงบท** (มีบท
   .txt/.docx อยู่แล้ว — แม่นกว่า และถ้ามีคนแก้บทใน Word มาให้ ใช้ทางนี้เลย)
3. แก้บท 3 แบบ (สามอย่างนี้**ต่างกัน**):
   - **แก้คำสะกด** = ดับเบิลคลิกที่คำ → พิมพ์ → Enter (เสียงไม่ถูกแตะ)
   - **ซ่อนคำจากบท** (อ่า/อืม/เสียงไอ) = คลิกขวาที่คำ (เสียงยังอยู่ แค่ไม่ไปอยู่ในเอกสาร)
   - **ตัดเสียง** = เลือกคำ → Delete (เสียง+ตัวหนังสือหายพร้อมกัน ย้อนได้ด้วย Undo)
4. **บันทึก** (Ctrl/Cmd+S) ได้ไฟล์งาน `.audioedit.json` — ส่งต่อให้เพื่อนได้
   (zip คู่กับไฟล์เสียงต้นฉบับในโฟลเดอร์เดียวกัน)
5. **Export** → เลือก "เสียง + บท" หรือ "เฉพาะบท" — ไฟล์ต้นฉบับไม่ถูกแก้เลยทุกกรณี

## 📨 ส่งงานให้เพื่อนทำต่อ (สำคัญ — อ่านให้ครบ)

### วิธีที่ง่ายที่สุด: ปุ่ม 📦 **รวมเป็นโปรเจกต์**

กดปุ่มนี้ปุ่มเดียว โปรแกรมจะสร้าง **โฟลเดอร์เดียวที่มีครบทั้งชุด** ให้เลย:

```
📁 story-project/
 ├── story.wav                  ← ไฟล์เสียงต้นฉบับ (ก๊อปมาให้)
 └── story.audioedit.json       ← ไฟล์งาน (ชี้ไปที่เสียงข้างๆ แบบไม่ผูกกับเครื่อง)
```

**ย้ายทั้งโฟลเดอร์ไปเครื่องไหนก็เปิดได้เลย** ไม่ต้องแตก zip ไม่ต้องจัดไฟล์เอง
ไม่มีปัญหา "หาไฟล์เสียงไม่เจอ" — จะบีบ zip ส่งต่อก็ได้ตามสะดวก

### วิธีเดิม (ถ้าจัดไฟล์เอง)

ไฟล์งาน `.audioedit.json` เก็บแค่ **ตัวหนังสือ + เวลา + รายการตัด** เท่านั้น
(ไฟล์เล็กมาก) — **ไม่มีไฟล์เสียงอยู่ข้างใน** ดังนั้นต้องส่ง **2 ไฟล์เสมอ**:

```
📁 โฟลเดอร์เดียวกัน (บีบ zip ส่งไป)
 ├── story.wav                    ← ไฟล์เสียงต้นฉบับ (ตัวจริง)
 └── story.wav.audioedit.json     ← ไฟล์งาน
```

**กติกา 3 ข้อ ที่ต้องทำตาม ไม่งั้นเพื่อนเปิดงานไม่ได้:**
1. **ส่งไฟล์เสียงต้นฉบับไปด้วยเสมอ** — โปรแกรมดึงเสียงจากไฟล์ .wav จริง
   ไม่ได้ฝังไว้ในไฟล์งาน
2. **วางสองไฟล์ไว้โฟลเดอร์เดียวกัน** — โปรแกรมจะหาไฟล์เสียงชื่อเดียวกัน
   ในโฟลเดอร์เดียวกับไฟล์งานให้อัตโนมัติ (ไม่ต้องลากรวมเอง)
3. **ห้ามเปลี่ยนชื่อไฟล์เสียง** — โปรแกรมค้นด้วยชื่อเดิม ถ้าเปลี่ยนชื่อจะหาไม่เจอ

**ฝั่งเพื่อน:** แตก zip → กดปุ่ม **"เปิดไฟล์เสียง"** → เลือกไฟล์ **.json**
→ โปรแกรมเจอไฟล์เสียงในโฟลเดอร์เดียวกันเอง เปิดงานครบทั้งบทและการตัด

> เครื่องเพื่อนที่แค่ตัด/ตรวจ/export **ไม่ต้องมีโมเดล AI** ก็เปิดงานนี้ได้
> (โมเดลจำเป็นเฉพาะตอน "ถอดเสียง/ตรึงบท" เท่านั้น)

**ถ้าขึ้น "หาไฟล์เสียงไม่เจอ":** แปลว่าไฟล์ .wav ไม่ได้อยู่โฟลเดอร์เดียวกับ
ไฟล์งาน หรือถูกเปลี่ยนชื่อ — เอาไฟล์เสียงต้นฉบับ (ชื่อเดิม) มาไว้ข้างไฟล์ .json
แล้วเปิดใหม่

**ส่งงานที่เสร็จแล้ว (ไม่แก้ต่อ):** ใช้ปุ่ม **Export** → เลือกได้ว่าจะเอา
**WAV** (คุณภาพเต็ม) หรือ **MP3** (ไฟล์เล็ก ส่งง่าย) พร้อม `.docx`/`.txt`
— อันนี้เป็นไฟล์ผลลัพธ์ ส่งให้คนที่ไม่ต้องเปิดในโปรแกรมได้เลย

## ↩️ เปลี่ยนใจ ไม่อยากตัดตรงนี้แล้ว

มี 2 วิธี เลือกตามสถานการณ์:

**1. เพิ่งตัดไปเมื่อกี้** → กด **Ctrl+Z** ย้อนได้เลย

**2. ตัดไปนานแล้ว ฟังทวนแล้วเพิ่งเปลี่ยนใจ** → **คลิกขวาที่แถบแดง**

- คลิกขวาบน **แถบสีแดงบนคลื่นเสียง** หรือ
- คลิกขวาบน **คำที่ขีดฆ่าสีแดงในบท**

เสียงตรงนั้นจะกลับมาทันที **โดยการตัดจุดอื่นๆ ยังอยู่ครบ** ไม่ต้องย้อนทีละขั้น

> ทำไมต้องมี 2 วิธี: Ctrl+Z ย้อน**ตามลำดับเวลาที่ทำ** ถ้าตัดไป 20 จุดแล้วอยาก
> คืนจุดที่ 5 ต้องย้อนทั้ง 15 จุดหลังทิ้งไปด้วย — คลิกขวาจึงเจาะจงกว่า
>
> ถ้าคลิกขวาผิดจุด กด **Ctrl+Z** ตัดกลับได้เหมือนเดิม

## 🚩 มาร์กจุด + เขียนโน้ต (ทำใบปะหน้า)

กด **M** ตรงตำแหน่งที่กำลังเล่นอยู่ → มีธงสีเหลืองปักบนคลื่น และมีแถวขึ้นมาให้
**พิมพ์โน้ต** ได้ทันที (เช่น "เสียงแตก" / "ต้องอัดใหม่")

- คลิกที่ **เวลา** ในรายการ = กระโดดไปฟังจุดนั้น
- **ลากธงบนคลื่น** = ขยับตำแหน่งได้
- ปุ่ม **📋 คัดลอกรายการ** = ได้ข้อความ `เวลา<แท็บ>โน้ต` ทั้งชุด เอาไปวางใน
  ใบปะหน้า/Excel ได้เลย
- ตอน Export บท รายการมาร์กจะต่อท้ายไฟล์ `.docx`/`.txt` ให้อัตโนมัติ
  ใต้หัวข้อ "— จุดที่มาร์กไว้ —"

> มาร์กเป็นแค่ **หมายเหตุ** ไม่แตะเสียง ไม่แตะการตัด ไม่ไปปนกับเนื้อหาบท

## ⌨️ ตัดเสียงด้วยคีย์บอร์ดล้วน (ไม่ต้องจับเมาส์)

ทำได้ครบเหมือน Sound Forge — ท่ามาตรฐานคือ:

1. **← →** เลื่อนหัวอ่านไปจุดที่จะเริ่มตัด (ซูมเข้าก่อนจะละเอียดขึ้น
   เพราะก้าวเดินเท่ากับ 1 พิกเซลบนจอ)
2. **Shift + →** กางแถบฟ้าออกทีละนิดจนคลุมช่วงที่ไม่เอา
   (**Shift + ←** เพื่อหดกลับ)
3. **Ctrl+K** ลองฟังก่อนว่ารอยต่อจะออกมายังไง
4. **Delete** ตัด — ถ้าไม่ถูกใจ **Ctrl+Z** ย้อนได้

> เก็บละเอียดกว่านี้ได้อีกด้วยแถบ **"เก็บละเอียดขอบตัด"** ที่โผล่ขึ้นมาใต้คลื่น
> (ปุ่ม ±1ms / ±10ms) หรือใช้ **Ctrl + ← →** ถ้าอยากกระโดดทีละวินาที

## 🔒 กั้นช่วงฟัง + ลองฟังรอยตัด (สำหรับคนที่ชินกับ Sound Forge)

**กั้นช่วงฟัง (Ctrl+L)** — เลือกช่วงที่ต้องการ แล้วกด → จากนี้กด **Space**
กี่ครั้ง เสียงก็เล่นอยู่แค่ในกรอบนั้น ไม่เลยออกไป กด **↻ วนลูป** เพิ่มก็วนซ้ำ
ให้ฟังไปเรื่อยๆ กดปุ่มเดิม/Ctrl+L ซ้ำเพื่อปลดกรอบ

**ลองฟังรอยตัด (Ctrl+K)** — เลือกช่วงที่กำลังจะตัด แล้วกด → โปรแกรมเล่นเสียง
**นำหน้า 1.5 วิ → ข้ามช่วงที่เลือก → เล่นต่ออีก 1.5 วิ** ให้ฟังว่ารอยต่อจะออกมา
เป็นยังไง **ก่อนตัดจริง** (ยังไม่มีอะไรถูกตัด)
ถ้าไม่ได้เลือกอะไรไว้ Ctrl+K จะพาไปฟังรอยตัดที่ทำไว้แล้ว จุดถัดไป

## ⚡ ถอดเสียงเร็วขึ้นด้วยการ์ดจอ

ถ้ามุมล่างขวาขึ้น **⚡ ใช้การ์ดจอ** สีเขียว = เครื่องนี้ถอดเสียงด้วยการ์ดจอ
NVIDIA แล้ว **เร็วกว่าซีพียูราว 10 เท่า** (วัดจริง: ไฟล์ 50 นาที จาก ~86 นาที
เหลือ ~8 นาที)

- ขึ้น **🖥️ มีการ์ดจอ แต่ยังไม่ได้เปิดใช้** สีเหลือง = เครื่องมีการ์ดจอแต่ยังไม่ได้
  ก๊อปไฟล์เสริมมาวาง → ดูวิธีใน [ติดตั้ง.md](ติดตั้ง.md)
- **ไม่ขึ้นอะไรเลย** = เครื่องนี้ไม่มีการ์ดจอ NVIDIA ใช้ซีพียูตามปกติ ไม่ได้พัง

> **ครั้งแรกหลังเปิดโปรแกรมจะช้ากว่าปกติมาก** (การ์ดต้องเตรียมโค้ดครั้งเดียว)
> หลังจากนั้นเร็วตลอด — อย่ากดยกเลิกระหว่างนั้น
>
> หมายเหตุ: การ์ดจอช่วยเฉพาะขั้น **"ถอดเสียง"** ส่วนขั้น **"ตรึงบท"** ยังใช้ซีพียู

## 🔎 ตรวจพลังเสียง (พิสูจน์ว่าไม่ได้บีบอัด)

ในหน้าต่าง Export มีช่อง **"ตรวจพลังเสียงหลัง export"** ติ๊กไว้ให้อยู่แล้ว
พอ export เสร็จ โปรแกรมจะเทียบ **RMS** (ความดังเฉลี่ย) และ **Peak** (ยอดสูงสุด)
ระหว่างต้นฉบับกับไฟล์ที่ตัดแล้ว แล้วบอกผลว่า

> 🔎 ตรวจแล้ว: พลังเสียงไม่เปลี่ยน (LUFS 0.00 dB, RMS 0.00 dB, peak 0.00 dB)

พร้อมเปิด**ตารางผลเต็ม**ให้ดู กดปุ่ม **📋 คัดลอกผล** เอาไปแปะในอีเมลหรือ
รายงานส่ง QC ได้เลย

นอกจากนี้ยังมีปุ่ม **🔎 เทียบพลังเสียง** บนแถบเครื่องมือ ใช้ดู**ก่อน export**
ว่างานที่ตัดแล้วจะมีค่าเท่าไหร่ โดยไม่ต้อง export ก่อน

### 📐 ถ้าจะตรวจซ้ำใน iZotope RX หรือ SpectraLayers

ตัวเลข **LUFS** และ **true peak** ของเราใช้มาตรฐานเดียวกับสองโปรแกรมนั้น
(ITU-R BS.1770-4) เอาไปวางเทียบกันได้ตรงๆ

> ⚠️ **อย่าเทียบทั้งไฟล์กับทั้งไฟล์**
>
> ค่าฝั่ง "ต้นฉบับ" ที่เราแสดง วัด**เฉพาะช่วงที่เก็บไว้** ไม่ใช่ทั้งไฟล์ —
> ต้องทำแบบนั้นถึงจะเทียบกับไฟล์ที่ตัดแล้วได้
>
> ถ้าเปิดต้นฉบับทั้งไฟล์ใน RX แล้วเอาไปลบกับไฟล์ที่ตัดแล้ว **ตัวเลขจะไม่ตรงกันแน่นอน**
> แต่ที่ไม่ตรง**เพราะเนื้อหาถูกตัดออกไป ไม่ใช่เพราะเสียงถูกบีบอัด** —
> RMS กับ LUFS คือค่าเฉลี่ย พอเอาบางช่วงออก ค่าเฉลี่ยก็เปลี่ยนเป็นธรรมดา
>
> **วิธีที่ให้ผลถูกต้อง:** เลือก**ช่วงที่ไม่ได้ถูกตัด**ในทั้งสองไฟล์
> แล้วเทียบเฉพาะช่วงนั้น ค่าจะตรงกันทุกหลัก เพราะเสียงที่เก็บไว้
> ถูกคัดลอกมาแบบบิตต่อบิต ไม่ผ่านการประมวลผลใดๆ

**สิ่งที่โปรแกรมรับประกัน:** ไม่มี normalize ไม่มีปรับความดัง ไม่มี compressor
sample rate และจำนวนช่องเท่าต้นฉบับ **และความละเอียด (bit depth) เท่าต้นฉบับ**
(24-bit เข้า → 24-bit ออก) สิ่งเดียวที่แตะคือ **crossfade 10 มิลลิวินาที**
ตรงรอยต่อ ซึ่งจำเป็น ไม่งั้นจะมีเสียง "แปะ"

> เลือก MP3 ตอน export จะเป็นการบีบอัดโดยธรรมชาติของ MP3 — ใช้ส่งงานได้
> แต่ไม่ควรใช้เป็นไฟล์ต้นฉบับ

## ↕️ ปรับขนาดช่องบท

ลาก**แถบคั่น**ระหว่างคลื่นเสียงกับช่องบทขึ้น-ลงได้ตามใจ โปรแกรม**จำขนาดไว้ให้**
เปิดครั้งหน้าได้ขนาดเดิม

- **ดับเบิลคลิก**ที่แถบคั่น = คืนค่าเดิม
- คลิกที่แถบแล้วกด **↑ / ↓** ปรับทีละนิด (กด Shift ด้วยจะปรับทีละมาก)

## เคล็ดการตัดให้คม

- ดูบน **spectrogram** (แถบล่าง): **แถบมืดแนวตั้ง = ช่วงเงียบระหว่างคำ** —
  ตัดตรงนั้นปลอดภัยสุด
- ระวัง**ฝ้าจางๆ ด้านบน**หลังคำที่ลงท้าย ส/ฟ/ช — คือหางเสียงที่ waveform
  มองไม่เห็น ตัดโดนแล้วคำจะขาดห้วน
- ปกติโปรแกรม snap ขอบเข้าช่วงเงียบให้อัตโนมัติ + ใส่ crossfade กันเสียงคลิก
  ทุกรอยต่ออยู่แล้ว — ปุ่ม **±1ms/±10ms** ใช้ตอนฟังแล้วยังไม่เนียน:
  ขยับทีละนิด → Shift+Space ฟังซ้ำ → **หูคือเกณฑ์ตัดสินสุดท้าย**

## ⌨️ ปุ่มลัดคีย์บอร์ด

> 💡 **ไม่ต้องจำก็ได้** — ในโปรแกรมมีปุ่ม **`? ปุ่มลัด`** มุมซ้ายบน กดดูได้ตลอด
> หรือกด **`?`** / **`F1`** บนคีย์บอร์ด

| ปุ่ม | ทำอะไร |
|---|---|
| **Space** | เล่น / หยุด |
| **Shift+Space** | ฟังเฉพาะช่วงที่เลือก |
| **Tab / Shift+Tab** | กระโดดไปคำขีดแดง (ที่ระบบไม่มั่นใจ) ถัดไป/ก่อนหน้า + เล่นเสียง + เปิดกล่องแก้ |
| **Enter** | เปิดกล่องแก้คำที่เลือก |
| **Delete / Backspace** | ตัดช่วงที่เลือก (ใช้ได้ทั้งเลือกคำ และลากแถบฟ้าบนคลื่น) |
| **← →** | เลื่อนหัวอ่านทีละ 1 พิกเซล — ยิ่งซูมเข้า ยิ่งละเอียด |
| **Ctrl/Cmd + ← →** | เลื่อนหัวอ่านทีละ 1 วินาที |
| **Shift + ← →** | **กาง/หดแถบฟ้า** ทีละ 1 พิกเซล (เลือกช่วงด้วยคีย์บอร์ดล้วน) |
| **Home / End** | ไปต้นไฟล์ / ท้ายไฟล์ |
| **Ctrl/Cmd + A** | เลือกทั้งไฟล์ |
| **Escape** | ยกเลิกการเลือก |
| **คลิกที่ว่างบนคลื่น** | วางหัวอ่านตรงนั้น + ยกเลิกแถบฟ้า |
| **Ctrl/Cmd+S** | บันทึกโปรเจกต์ |
| **Ctrl/Cmd+F** | ค้นหาคำ |
| **Ctrl/Cmd+Z** | ย้อนทีละขั้น **ตามลำดับที่ทำ** (Undo) |
| **คลิกขวาที่แถบแดง** | **เอาการตัดเฉพาะจุดนั้นคืน** — ไม่ต้องย้อนทั้งหมด |
| **Ctrl/Cmd+Shift+Z** | ทำซ้ำ (Redo) |
| **Ctrl/Cmd+K** | **ลองฟังรอยตัด** — เล่นนำ 1.5 วิ ข้ามช่วงที่เลือก แล้วเล่นต่อ 1.5 วิ (เหมือน Preview Cut ของ Sound Forge) ถ้าไม่ได้เลือกอะไร จะฟังรอยตัดที่ทำไว้แล้ว |
| **Ctrl/Cmd+L** | **กั้นช่วงฟัง** — ล็อกให้ Space เล่นเฉพาะช่วงที่เลือก กดซ้ำเพื่อปลด |
| **M** | **มาร์กจุด** ตรงตำแหน่งที่เล่นอยู่ แล้วพิมพ์โน้ตได้เลย |
| **ล้อเมาส์** (บนคลื่นเสียง) | ซูมเข้า/ออก |
| **Ctrl/Cmd+ล้อเมาส์** | ซูมเข้า/ออก (เหมือนกัน — ท่า pinch บน trackpad Mac ก็อันนี้) |
| **Shift+ล้อเมาส์** | เลื่อนคลื่นไปซ้าย/ขวา |
| **Alt+ล้อเมาส์** | เลื่อนขึ้น/ลง ดู spectrogram |

## 🖱️ เมาส์บนตัวหนังสือ

| ท่า | ทำอะไร |
|---|---|
| คลิกคำ | เลือกคำ (เสียงเด้งตาม ไม่เล่นเอง — จะฟังกด Space) |
| Shift+คลิก | เลือกตั้งแต่คำที่เลือกไว้ถึงคำนี้ |
| ลากกวาด | เลือกหลายคำ |
| ดับเบิลคลิก | แก้คำสะกด (เสียงไม่ถูกแตะ) |
| คลิกขวา | ซ่อนคำจากบท (เสียงยังอยู่) |
| ปุ่ม ✎ หน้าวรรค | แก้ทั้งวรรคอิสระ แล้วระบบตรึงเวลาใหม่ให้ |

## 🖱️ เมาส์บนคลื่นเสียง (waveform + spectrogram)

| ท่า | ทำอะไร |
|---|---|
| คลิก | ย้ายหัวอ่านไปตรงนั้น |
| ลากบนที่ว่าง | เลือกช่วงเสียง (ไม่ต้องมีบทก็ตัดได้ — ตัดหยาบก่อนถอด) |
| ลากขอบแถบฟ้า/แดง | ปรับขอบช่วงเลือก/ขอบตัด (ทำได้ทั้งสองมุมมอง) |
| ปุ่ม ±1ms ±10ms (แถบล่าง) | ขยับขอบตัดละเอียดทีละนิด |

## 🎨 ความหมายสี/สัญลักษณ์

| เห็น | คือ |
|---|---|
| คำขีดเส้นแดง | ระบบไม่มั่นใจ ควรตรวจ (กด Tab ไล่ได้) |
| คำจาง+ขีดทิ้ง | ถูกซ่อนจากบท (เสียงยังอยู่) |
| แถบฟ้าบนคลื่น | ช่วงที่เลือก (ยังไม่ตัด) |
| แถบแดงบนคลื่น | ช่วงที่ตัดแล้ว (ย้อนได้ด้วย Ctrl/Cmd+Z) |
| ● ยังไม่บันทึก | มีงานค้าง อย่าลืม Ctrl/Cmd+S |
| จุดเขียว "backend: connected" (มุมล่าง) | ตัวประมวลผลพร้อมทำงาน — ถ้าแดง ปิดโปรแกรมแล้วเปิดใหม่ |
