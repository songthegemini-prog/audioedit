# FIXES.md — บันทึกปัญหาที่เคยแก้แล้ว

กติกา: เจอบั๊กใหม่หรือแก้พฤติกรรมที่ผู้ใช้เคยรายงาน → **เพิ่มบันทึกที่นี่เสมอ**
(อาการ / สาเหตุ / วิธีแก้ / วิธีกันไม่ให้เกิดซ้ำ) และก่อนแตะโค้ดส่วนไหน
ให้กวาดตาดูว่าส่วนนั้นเคยมีประวัติอะไร

---

## 1. คลิกคำแล้ว "เสียงไม่ไป" (รายงาน 2 รอบ)

- **อาการ:** คลิกคำใน transcript แล้วรู้สึกว่าไม่มีอะไรเกิดขึ้น
- **สาเหตุ (2 ชั้น):**
  1. WaveSurfer เลื่อนจอตามเคอร์เซอร์เฉพาะตอน "กำลังเล่น" — ตอนหยุด playhead
     กระโดดไปนอกจอโดยจอไม่เลื่อนตาม (แก้ commit `7a362da`)
  2. คลิกคำเดิมทีแค่ "ย้ายตำแหน่ง" ไม่เริ่มเล่นเสียง — ตอนเสียงหยุดอยู่ คลิกแล้วเงียบ
     ผู้ใช้จึงมองว่า "เสียงไม่ไป" (แก้ให้คลิก = เล่นจากคำนั้นทันที)
- **วิธีแก้:** `player.playFrom()` = setTime + setScrollTime + play
- **บทเรียนซ้อน (รอบ 3):** ตอนแรกกันดับเบิลคลิกด้วยการหน่วงคลิกเดี่ยว 230ms —
  ผู้ใช้ได้ยินเป็น "เสียงค้างคำเดิมแป๊บนึงแล้วค่อยกระโดด" ทันที **ห้ามหน่วงการ seek
  ทุกกรณี** วิธีที่ถูกคือ seek ทันที แล้วให้การเข้าโหมดแก้คำ (dblclick) สั่ง pause
  เสียงแทน (`onEditStart` → `player.pause()`)
- **กันเกิดซ้ำ:** ถ้าแตะ `seekTo`/`playFrom`/click handler ของ token ต้องทดสอบ
  ด้วยมือทุกครั้ง: คลิกคำตอนหยุด (ได้ยินทันที + จอเลื่อน), คลิกคำใหม่ตอนกำลังเล่น
  (กระโดดทันที ไม่มีหางคำเดิมค้าง), ดับเบิลคลิก (เสียงหยุด + เข้าโหมดแก้คำ)
- **⚠ อัปเดตสำคัญ (Phase 5a):** ผู้ใช้เปลี่ยนโมเดลการคลิกเป็นแบบ text editor —
  ดูข้อ 8; "คลิก = เล่นเสียง" ถูกยกเลิกโดยตั้งใจ **อย่า restore กลับ**

## 2. Waveform แบนจนมองไม่เห็นกับไฟล์เสียงเบา

- **อาการ:** ไฟล์เสียงพากย์จริงเห็นคลื่นเป็นเส้นเตี้ยๆ ยิ่งซูมยิ่งดูไม่ออก
- **สาเหตุ:** WaveSurfer วาดตามแอมพลิจูดจริง ไฟล์อัดเบาเลยแบน (ซูมขยายแค่แกนเวลา)
- **วิธีแก้:** `normalize: true` ใน `src/audio/player.ts` (commit `357df42`) —
  เป็นการขยาย "ภาพวาด" เท่านั้น ไม่แตะสัญญาณเสียงจริง
- **กันเกิดซ้ำ:** ห้ามถอด option นี้ออก; ถ้าไฟล์มี spike ดังจุดเดียวแล้วที่เหลือเตี้ย
  ให้ทำปุ่ม vertical gain เพิ่ม (ยังไม่ทำ)

## 3. Pathumma ตัดคำ filler ทิ้งจาก transcript เงียบๆ

- **อาการ:** ถอดด้วย Pathumma แล้ว เอ่อ/อือ หายไปจากข้อความทั้งที่มีอยู่ในเสียง
- **สาเหตุ:** โมเดลถูก fine-tune ให้ผลิตข้อความสะอาด → ไม่มี token ของ filler
  ให้ผู้ใช้คลิกหา/ตัด ขัด workflow หลักและกติกา CLAUDE.md
- **วิธีแก้:** Thonburian เป็น default; Pathumma เป็น opt-in ผ่าน
  `AUDIOEDIT_MODEL_DIR` เท่านั้น (commit `16bebaa`, มีเทสต์
  `test_default_is_thonburian_even_when_pathumma_exists` ล็อกพฤติกรรมไว้)
- **กันเกิดซ้ำ:** ประเมินโมเดล ASR ใหม่ทุกตัวด้วยไฟล์ที่มี filler ก่อนตั้งเป็น default
  (`samples/thai-test.wav` มี เอ่อ/อือ ฝังไว้เพื่อการนี้)

## 4. แปลง Pathumma แล้วพังเพราะไฟล์ประกอบไม่ครบ

- **อาการ:** `convert_pathumma.py` ล้ม: `tokenizer.json does not exist`;
  แก้แล้วยังขาด `preprocessor_config.json` (จำเป็น — large-v3 ใช้ 128 mel bins
  ถ้าไม่มีเสียงจะถอดเพี้ยนทั้งไฟล์)
- **สาเหตุ:** repo ของ NECTEC ไม่แนบ tokenizer.json และ transformers v5
  เปลี่ยนชื่อไฟล์ที่ save จาก AutoProcessor
- **วิธีแก้:** สคริปต์ regenerate ทั้งสองไฟล์ด้วย `AutoTokenizer` +
  `AutoFeatureExtractor.save_pretrained()` (commit `16bebaa`)
- **กันเกิดซ้ำ:** หลังแปลงโมเดลใดๆ ให้เช็คว่ามีครบ: `model.bin`, `config.json`,
  `tokenizer.json`, `preprocessor_config.json` แล้วถอดไฟล์สั้นทดสอบเสมอ

## 5. ASR ได้ยิน "เอ่อ" เป็น "เออ" แล้วไม่ถูกติดธง filler

- **อาการ:** คำ filler ในผลถอดจริงไม่ถูกไฮไลท์เหลือง
- **สาเหตุ:** lexicon มี เอ่อ/เอ้อ แต่ไม่มีการสะกด "เออ" ที่ ASR ชอบใช้
- **วิธีแก้:** เพิ่ม "เออ" ใน `DEFAULT_FILLER_WORDS` (commit `cccc2ef`)
- **กันเกิดซ้ำ:** เจอการสะกด filler แบบใหม่ในผลถอดจริง → เพิ่ม lexicon ใน
  `backend/app/tokens.py` + เทสต์; จำไว้ว่า filler ถูก "ติดธง" ไม่ใช่ "ลบ"

## 6. ถอดเสียงช้ามาก (0.3x เรียลไทม์)

- **อาการ:** เสียง 4 นาทีใช้เวลาถอดกว่า 10 นาที
- **สาเหตุ:** ถอดทีละท่อน + beam_size 5
- **วิธีแก้:** `BatchedInferencePipeline` + beam 2 → เร็วขึ้น ~10 เท่า
  (165 วิ: 629→65 วิ) commit `16bebaa`; จูนได้ผ่าน `AUDIOEDIT_BEAM_SIZE` /
  `AUDIOEDIT_BATCH_SIZE` / `AUDIOEDIT_CPU_THREADS`
- **กันเกิดซ้ำ:** ถ้าอัปเกรด faster-whisper แล้วช้าลง ให้เทียบเวลากับตัวเลขนี้
  (สคริปต์วัดอยู่ในประวัติ session; ไฟล์ทดสอบ `samples/thai-long.wav` 165 วิ)

## 7. เสียงกับภาพคลื่นเหลื่อมกัน (คลิกหัวก้อนเสียง ได้ยินก้อนถัดไป)

- **อาการ:** ตำแหน่งบน waveform กับเสียงที่ได้ยินไม่ตรงกันคงที่ทั้งไฟล์ (เจอกับ m4a)
- **สาเหตุ:** waveform วาดจาก WebAudio decode แต่เสียงเล่นผ่าน media element —
  กับไฟล์ AAC/m4a สองเส้นทางนี้จัดการ codec priming / edit list ที่หัวไฟล์ต่างกัน
  → timeline สองอันเหลื่อมกัน
- **วิธีแก้:** `backend: "WebAudio"` ใน WaveSurfer.create (`src/audio/player.ts`) —
  เล่นจาก buffer เดียวกับที่วาดภาพ เหลื่อมกันไม่ได้โดยนิยาม
- **กันเกิดซ้ำ:** ห้ามเปลี่ยนกลับเป็น media element โดยไม่ทดสอบกับไฟล์ m4a จริง;
  ตอนทำเฟส long-audio (streaming/visible-window) ต้องคง "แหล่งข้อมูลเดียวกัน
  สำหรับภาพและเสียง" เป็นหลักการออกแบบ

## 8. เลือกคำเพื่อตัดลำบาก + เสียงไหลระหว่างเลือก (Phase 5a)

- **อาการ:** ผู้ใช้รายงาน: ลากเลือกยาก, คลิกคำแล้วเสียงเล่นทำให้เลือกไม่สะดวก,
  บางครั้ง selection ไม่ขึ้นบน waveform
- **สาเหตุ:** โมเดล "คลิก = เล่นเสียง" (เหมาะกับเฟสฟัง) ขัดกับงานตัดที่ต้อง
  เลือกซ้ำหลายร้อยครั้ง; plain click เคลียร์ selection ทำให้แถบฟ้าหาย
- **วิธีแก้ (ดีไซน์ที่ผู้ใช้เลือกเอง):** โมเดลแบบ text editor —
  **คลิกคำ = เลือกคำนั้นทันที + เสียงหยุด + playhead ย้ายไปหน้าคำ**;
  Shift+คลิก/ลาก = ขยายช่วง; ฟังต้องสั่งชัดๆ (Space หรือปุ่มฟังช่วงที่เลือก)
- **กันเกิดซ้ำ:** นี่คือดีไซน์ที่ผู้ใช้ยืนยันแล้ว ห้ามเปลี่ยนกลับเป็น click=play;
  ทดสอบมือ: คลิกคำเดียว (เลือก+เงียบ+แถบฟ้าขึ้นทั้ง transcript และ waveform),
  Shift+คลิก (ขยาย), Space (เล่นจากหน้าคำที่เลือก), Delete (ตัดช่วงที่เลือก)

## 9. แถบเลือกไม่ครอบหัวเสียง + ผู้ใช้ปรับขอบเองไม่ได้ (Phase 5a)

- **อาการ:** แถบฟ้าเริ่มช้ากว่าหัวเสียงจริง (เวลาจาก alignment มักตามหลัง onset
  เล็กน้อย) และขอบแถบฟ้าลากปรับไม่ได้
- **วิธีแก้:** snap ขอบตั้งแต่ตอน "สร้างแถบเลือก" (ไม่รอตอนกดตัด) → สิ่งที่เห็น =
  สิ่งที่จะตัดจริง; เปิด resize ให้แถบฟ้า และ**ถ้าผู้ใช้ลากขอบเอง ระบบห้าม
  snap ทับมือ** — ใช้ค่าที่ผู้ใช้ตั้งตรงๆ ทั้งตอนฟังช่วงและตอนตัด
- **กันเกิดซ้ำ:** หลักการ "แถบที่เห็นต้องเท่ากับช่วงที่จะตัดจริงเสมอ" และ
  "ค่าที่ผู้ใช้ปรับด้วยมือชนะระบบอัตโนมัติเสมอ" — ใช้กับ 5b (ลากบน spectrogram) ด้วย

## 10. Spectrogram เบลอตอนซูม + โหลดช้า (รายงานตั้งแต่ Phase 1, ปิดหนี้ใน 5b)

- **อาการ:** ซูมลึกแล้ว spectrogram เป็นแถบเลอะ อ่านไม่ได้ และไฟล์ยาวโหลดช้า
- **สาเหตุ:** ปลั๊กอิน WaveSurfer คำนวณ FFT ทั้งไฟล์ครั้งเดียว (หน้าต่างคงที่)
  แล้วยืดภาพตามซูม ไม่คำนวณใหม่
- **วิธีแก้:** เขียนเอง (`src/audio/spectrogram.ts` + `fft.ts`): คำนวณ 1 คอลัมน์
  FFT ต่อ 1 พิกเซลของช่วงที่มองเห็น, hop/ขนาดหน้าต่างปรับตามซูมอัตโนมัติ,
  วาดใหม่ทุกครั้งที่ scroll/zoom → คมทุกระดับ เปิดไฟล์ไว (ไม่มี FFT ทั้งไฟล์อีก)
- **กันเกิดซ้ำ:** ห้ามกลับไปใช้ปลั๊กอิน Spectrogram ของ WaveSurfer;
  ถ้าช้าเมื่อ viewport กว้างมาก ให้ลด fftSize สูงสุด ไม่ใช่กลับไป precompute ทั้งไฟล์

## 11. กดเล่นแล้วเงียบสนิท (หลังเคยใช้ "ฟังช่วงที่เลือก")

- **อาการ:** กด Space/เล่น แล้วไม่ได้ยินอะไรเลย
- **สาเหตุ:** `playRange` ตั้งจุดหยุดอัตโนมัติ (`rangeEnd`) แล้วไม่ถูกล้าง —
  พอเล่นปกติที่ตำแหน่งหลังจุดนั้น ระบบสั่ง pause ทันทีในเฟรมแรก
- **วิธีแก้:** ล้าง `rangeEnd` ใน `playPause()` และ `seekTo()` เสมอ;
  `playRange` ตั้งค่าหลังจาก seek (`src/audio/player.ts`)
- **กันเกิดซ้ำ:** state ชั่วคราวของโหมดเล่นพิเศษใดๆ (range stop, skip cuts)
  ต้องถูกล้าง/ทบทวนในทุกทางเข้าของการเล่นปกติ; ทดสอบมือ: ฟังช่วงที่เลือก →
  จบ → คลิกที่อื่น → Space ต้องเล่นปกติ

## 12. ตรึงบทแล้วบรรทัดยืดเกินจริงจน cursor วิ่งชนท้ายไฟล์ (จับได้ตอน verify Phase 6)

- **อาการ:** align ทีละบรรทัดด้วยหน้าต่างค้นหากว้าง → แต่ละบรรทัดยืด ~2 เท่า
  บรรทัดท้ายๆ เวลาเป็น 0 ทั้งหมด
- **สาเหตุ:** `forced_align` เกลี่ย target ให้เต็ม "ทั้งหน้าต่าง" เสมอ — ถ้าหน้าต่าง
  มีเสียงของบรรทัดถัดไปปน คำท้ายจะถูกดูดไปทับ และ error สะสมทุกบรรทัด
- **วิธีแก้:** align เป็น batch ~35 วิ (หลายบรรทัดพร้อมกัน หน้าต่างแน่น est×1.2+3s)
  ใน `backend/app/align_script.py` — ขอบระหว่างบรรทัดถูกบังคับด้วยคำจริงทั้งชุด
- **กันเกิดซ้ำ:** ห้าม align "ข้อความสั้นในหน้าต่างกว้าง"; ถ้าบทมีเนื้อความซ้ำกัน
  เป๊ะหลายช่วง (เช่นท่อนซ้ำ) aligner อาจจับผิดช่วง — บรรทัดที่ conf ต่ำคือสัญญาณ;
  บรรทัดที่ผิดจริง 1 บรรทัดอาจลาก conf บรรทัดข้างเคียงใน batch เดียวกันต่ำลงด้วย
  (ตั้งใจ — ให้คนตรวจทั้งกลุ่ม)

## 13. เปิดไฟล์แล้วเงียบสนิททั้งไฟล์ (playhead ไม่ขยับด้วย)

- **อาการ:** บางเซสชันกดเล่นแล้วไม่มีเสียงเลยกับทุกท่า (เจอกับ story.wav)
- **สาเหตุ:** WebKit ระงับ (suspend) AudioContext ที่ถูกสร้างก่อนผู้ใช้คลิกครั้งแรก
  และ WebAudioPlayer ของ wavesurfer **ไม่เคยเรียก resume() เลย** → เสียงเงียบ
  และนาฬิกา context หยุดเดิน (เลยดูเหมือนแอปค้าง) — อาการเกิดๆ หายๆ ตามจังหวะ
  ว่า context ถูกสร้างก่อนหรือหลัง gesture แรก
- **วิธีแก้:** `ensureAudioRunning()` ใน `src/audio/player.ts` — เช็ค
  `audioContext.state === "suspended"` แล้ว `resume()` ในทุกทางเข้าของการเล่น
  (playPause / playFrom / playRange ล้วนถูกเรียกจาก user gesture อยู่แล้ว)
- **กันเกิดซ้ำ:** เพิ่มทางเข้าการเล่นใหม่เมื่อไหร่ ต้องเรียก ensureAudioRunning ก่อน
- **⚠ รอบ 2 (เสียงหายซ้ำแม้มี resume):** WebAudio ของ WebKit ดับเองได้ทั้งที่สถานะ
  "running" (เช่น หลังสลับอุปกรณ์เสียง) → **เลิกใช้ WebAudio เล่นเสียงถาวร**:
  ถอดรหัสครั้งเดียว → encode เป็น WAV (src/audio/wav.ts) → เล่นผ่าน media element
  ธรรมดาซึ่งเสถียรใน WebKit; ภาพ/เสียง sync กันโดยโครงสร้างเพราะมาจาก buffer
  ถอดรหัสชุดเดียวกัน (คงหลักการข้อ #7) — **กติกาใหม่: playback ต้องกินข้อมูล
  PCM ชุดเดียวกับที่ใช้วาดภาพเสมอ ไม่ว่าจะเล่นผ่านกลไกไหน**

## 14. ปุ่มติดตั้งโมเดล error HTTP 404 (แอปแพ็กแล้วเกาะ backend ผิดตัว)

- **อาการ:** ในแอปที่ build แล้ว กด "ติดตั้งโมเดล" ขึ้น "ดาวน์โหลดไม่สำเร็จ:
  Error: HTTP 404"
- **สาเหตุ:** มี dev uvicorn เก่า (โค้ดก่อน Phase 8 ไม่มี /download_models)
  ค้างอยู่พอร์ต 8000; ตอนแอปเปิด sidecar (8756) ยังบูตไม่เสร็จ ~10 วิ →
  health probe ไปเจอ 8000 ก่อนแล้ว **ล็อก backendUrl ติดตัวเก่าถาวร** →
  /download_models บนตัวเก่า = 404
- **วิธีแก้:** `health()` ใน src/api.ts re-probe ตามลำดับความสำคัญ (8756 ก่อน
  8000) **ทุกครั้ง** ไม่ล็อกติดตัวที่ตอบก่อน — sidecar ชนะเสมอเมื่อบูตเสร็จ
- **กันเกิดซ้ำ:** ห้ามเปิด dev uvicorn ค้างไว้ตอนทดสอบตัว build; ถ้าเพิ่มพอร์ต
  candidate ใหม่ ต้องเรียงตามลำดับที่ prod ต้องชนะ dev เสมอ

## 15. ปิดแอปแล้ว backend sidecar ยังค้าง (process ผีกิน RAM/พอร์ต)

- **อาการ:** quit แอป (.app build แล้ว) แต่ audioedit-backend ยังรันค้าง —
  เปิดแอปใหม่ชนพอร์ต 8756 หรือกิน RAM สะสม
- **สาเหตุ:** kill child ผูกกับ `WindowEvent::Destroyed` ซึ่งไม่ยิงเมื่อ quit
  ทั้งแอป (Cmd+Q) — process หลักตายก่อน handler ทำงาน
- **วิธีแก้:** ย้ายไปใช้ `app.run(|handle, event| { if RunEvent::Exit {...} })`
  ใน src-tauri/src/lib.rs — Exit ยิงชัวร์ทุกทางออก (quit/close/window) แล้ว
  kill+wait ตัว child
- **กันเกิดซ้ำ:** การจัดการ lifecycle ของ sidecar ต้องผูกกับ RunEvent::Exit
  ไม่ใช่ window event; ทดสอบทุก build: quit แล้ว `pgrep audioedit-backend`
  ต้องเป็น 0

## 16. แถบ "ติดตั้งโมเดล" ค้างทั้งที่โมเดลมีครบ

- **อาการ:** แอป build แล้ว โชว์แถบเหลือง "ยังไม่มีโมเดล AI" ค้าง แม้ปิด-เปิดใหม่
  ทั้งที่ backend (curl ตรงๆ) ตอบ asr:true align:true
- **สาเหตุ:** frontend เช็คโมเดล "ครั้งเดียว" ด้วย flag `modelsChecked` — sidecar
  ตัว frozen ใช้เวลาบูต (import torch) นาน; ถ้าจังหวะเช็คแรกไปโดนช่วงพร่อง แล้ว
  latch ผลค้าง จะไม่เช็คซ้ำอีกเลย แถบเลยค้าง
- **วิธีแก้:** เช็คโมเดลซ้ำทุกรอบ health poll ตราบใดที่ยัง `!modelsReady`
  (หยุดเองเมื่อเจอ; ข้ามถ้ามี download job กำลังรัน) ใน src/main.ts
- **กันเกิดซ้ำ:** อย่า latch สถานะที่ขึ้นกับ sidecar ตอนบูต — poll จนกว่าจะได้
  สถานะที่ถูก; ทดสอบ: เปิดแอปที่มีโมเดลครบ แถบต้องหายเองใน ~10 วิ

## 17. แถบ "ติดตั้งโมเดล" โชว์ตลอด ซ่อนไม่ลง (CSS override attribute hidden)

- **อาการ:** แถบเหลืองค้างแม้โมเดลมีครบ + backend ตอบ true + โค้ดสั่ง
  `modelsBanner.hidden = true` แล้ว; ล้าง cache/rebuild หลายรอบไม่หาย
- **สาเหตุ (ตัวจริง — ไม่ใช่ cache!):** `.models-banner { display: flex }`
  มี specificity เท่ากับ UA rule `[hidden] { display: none }` แต่มาทีหลัง →
  ชนะ → element โชว์ตลอดไม่ว่า attribute hidden จะเป็นอะไร (แถบ .fine-bar
  ก็โดนด้วย). อาการหลอก: ปุ่มทำงาน (modelsReady=true), แต่ CSS ดันโชว์ทับ
- **วิธีแก้:** `[hidden] { display: none !important; }` ที่ต้น styles.css —
  บังคับ hidden ชนะเสมอ (ครอบทุก element ที่ toggle ด้วย .hidden)
- **กันเกิดซ้ำ:** อย่าตั้ง `display:` ให้ element ที่ toggle ด้วย attribute
  `hidden` โดยไม่มี guard; มี `[hidden]{display:none!important}` global แล้ว
  แต่ถ้าเห็น element "ซ่อนไม่ลง" ให้สงสัย CSS override ก่อน cache

## 18. แถบเลือกสีฟ้า "ค้าง" คลุมยาวถึงท้ายไฟล์ ลบไม่ออก (orphan drag-region)

- **อาการ:** ลากเลือกบน waveform แล้วบางครั้งมีแถบฟ้าแถบที่สองคลุมยาวไป
  จนสุดไฟล์ และค้างอยู่ไม่หายไม่ว่ากดอะไร (เจอทั้งไฟล์สั้น/ยาว ทุกระดับซูม)
- **สาเหตุ:** ปลั๊กอิน regions ของ wavesurfer แปะ element ของ region ที่เกิด
  จากการลากลง DOM ตั้งแต่ "เริ่มลาก" แต่ลงทะเบียนเข้า list ก็ต่อเมื่อจังหวะ
  "ปล่อยเมาส์" จบสมบูรณ์เท่านั้น — ถ้ามีเหตุแทรกกลางลาก (นิ้ว/ฝ่ามือแตะ
  trackpad ซ้อนเป็น pointer ที่สอง, สลับหน้าต่าง ฯลฯ) event จบหาย →
  element กลายเป็นกำพร้า: มองเห็นถาวรแต่ clearRegions()/remove() เข้าไม่ถึง
- **วิธีแก้ (2 ชั้น):**
  1. `sweepOrphanRegions()` ใน player.ts — เทียบ element ใน regions
     container กับ region ที่ระบบรู้จัก แล้วลบตัวแปลกปลอมทิ้งทุกจังหวะ
     อัปเดต region (ตาข่ายนิรภัย)
  2. **เลิกใช้ enableDragSelection ของปลั๊กอินทั้งหมด** — ผู้ใช้ยืนยันว่า
     แถบบานเกิด "ทุกครั้ง" ที่ลากใน .app (WKWebView) = คณิตกลางลากของ
     ปลั๊กอินพังบนเอนจินนั้นอย่างเป็นระบบ → เขียน drag-select เอง
     (`initDragSelection` ใน player.ts, แพตเทิร์นเดียวกับ spectrogram):
     เวลา = สัดส่วน x ใน wrapper × duration สูตรเดียว มี clamp — บานเกิน
     ตำแหน่งเมาส์ไม่ได้เชิงโครงสร้าง; กลืน click ที่ตามหลัง drag กันเด้ง seek
- **กันเกิดซ้ำ:** พฤติกรรม interaction สำคัญ (ลาก/คลิก) อย่าพึ่ง logic
  ภายใน plugin ที่ทดสอบบน WKWebView ไม่ได้ — เขียนเองด้วยสูตรที่คุมได้
  แล้วเทสต์ค่าตัวเลขจริง; ถ้าเห็น region "ลบไม่ออก" ให้สงสัย element
  นอก list ก่อน
- **ตัวการจริง (พบ 2026-07-12 จากปุ่มวินิจฉัย Cmd+Shift+D + วัดสีพิกเซล):**
  แถบฟ้า "อ่อน" ที่คลุมจากต้นไฟล์ถึงหัวอ่าน **ไม่ใช่ region เลย** — เป็น
  **progress canvas ของ wavesurfer** (progressColor `#8ab4f8` = 138,180,248)
  ที่ระบายช่วง "เล่นไปแล้ว/seek ไปแล้ว"; ชนสีกับ selection สีฟ้า เลยดูเหมือน
  แถบเลือกค้าง. แก้: ตั้ง `progressColor === waveColor` (#5b8dbb) — โปรแกรม
  ตัดต่อไม่ควรระบายช่วงที่เล่นแล้ว ใช้เส้น cursor ขาวบอกตำแหน่งพอ (setWaveHidden
  restore ก็ตั้งให้เท่ากัน). บทเรียน: สีที่ผู้ใช้เห็นให้ "วัดค่าพิกเซลจริง"
  อย่าเดาจาก DOM — regions บริสุทธิ์มาตั้งแต่ต้น

## 19. กด "งานใหม่" แล้วตัวเลขความยาวไฟล์เดิมยังค้าง (0:00.0 / 54:12.5)

- **อาการ:** เริ่มงานใหม่แล้ว จอเวลายังโชว์ความยาวของไฟล์ก่อนหน้า
- **สาเหตุ:** newProject พึ่ง ws.empty() ให้รีเซ็ตจอ แต่ empty() ของ
  wavesurfer คือ load(URL ว่าง) ซึ่ง fail เงียบ → event "ready" ที่จะพา
  updateTime มาไม่เคยยิง → ข้อความเก่าค้าง
- **วิธีแก้:** newProject เซ็ต timeDisplay เป็น "0:00.0 / 0:00.0" ตรงๆ
  (src/main.ts) ไม่รอ event
- **กันเกิดซ้ำ:** ปุ่ม "งานใหม่" ต้องรีเซ็ต UI ทุกชิ้นแบบ explicit — ห้ามพึ่ง
  side effect จาก wavesurfer; เพิ่มรายการเช็ค: ชื่อไฟล์, เวลา, ตัวนับตัด,
  ตัวนับตรวจ, transcript, แถบเลือก, zoom

## 20. หลังใช้ค้นหา/Tab แล้ว "คลิกคำไหนก็เงียบ" ไม่ไฮไลต์เสียงอีกเลย (edit-box latch)

- **อาการ:** ใช้ค้นหาแล้วกดคำที่เจอ → ไม่เด้งไปที่คำ ไม่ไฮไลต์เสียง และหลัง
  จากนั้นคลิกคำไหนก็เงียบทั้งไฟล์ จนต้องเปิดไฟล์/แอปใหม่
- **สาเหตุ:** กล่องแก้คำ (input) อาศัยอยู่ "ใน" span ของคำ และปิดตัวด้วย
  blur/Enter เท่านั้น — แต่ `refresh(i)` เขียน `span.textContent = ...` ซึ่ง
  **ทำลาย input โดย blur ไม่ยิง** (ลบ element ที่โฟกัสอยู่ browser ไม่ส่ง
  blur) → ธง `editing` ค้าง true ตลอดกาล → ทุกคลิก/ดับเบิลคลิกโดนประตู
  `if (editing) return` กลืนหมด; มีหลายทางที่ refresh โดน span ที่กล่องเปิด:
  ไฮไลต์คำตามเสียง (Tab เปิดกล่อง+เล่นเสียง → เสียงพ้นคำ → refresh),
  ผลค้นหา (setSearchMatches), ซ่อน filler (refreshAll)
- **วิธีแก้ (3 ชั้น ใน transcript.ts):** (1) `refresh()` ห้ามทับ span ที่มีกล่อง
  เปิด (2) ประตู editing ทุกจุดใช้ `editingActive()` ที่เช็คว่า editor element
  ยังอยู่ใน DOM จริง — หายไป = ปลดธงเอง (self-healing) (3) `render()/clear()`
  รีเซ็ตธงเสมอ
- **กันเกิดซ้ำ:** UI ที่ "ฝังใน DOM ที่คนอื่น repaint ได้" ห้ามถือ state ด้วย
  ธงลอยๆ — ต้องผูกกับ element จริง (isConnected) และ repaint ต้องเว้นจุดที่
  กำลังแก้; ทดสอบ: Tab เปิดกล่อง → ปล่อยเสียงเล่นจนพ้นคำ → กล่องต้องยังอยู่
  และพิมพ์ต่อได้

## 21. โหมดไฟล์ยาว: คลิกคำแล้วค้างถาวรถ้า request เสียงภายในสะดุด (no timeout)

- **อาการ (ศักยภาพ — เสริมจาก #20):** ในไฟล์ยาว การคลิกคำต้องดึง PCM
  ผ่าน /pcm มา snap ขอบ — fetch ไม่มี timeout: ถ้า request ค้าง promise
  ค้างใน cache ของ RemoteSamples → คลิกแถวนั้นรอชั่วนิรันดร์ และ connection
  ที่ค้างสะสมกิน pool ต่อ origin (6 ตัว) จนทุก request แช่แข็งทั้งแอป
- **วิธีแก้:** AbortSignal.timeout ใน api.ts — /pcm 8s, /peaks 15s,
  /audio_info 5s; ทุกชั้นบนมี catch รองรับอยู่แล้ว (snap ไม่ได้ = ใช้ขอบดิบ,
  cache ลบ promise ที่ fail เอง)
- **กันเกิดซ้ำ:** fetch ทุกตัวที่ UI ต้อง "รอ" ก่อนตอบสนองผู้ใช้ ต้องมี
  timeout เสมอ — ห้ามปล่อย pending ไม่จำกัดเวลา

## 22. หลังกดปุ่มใดๆ แล้ว Space ไม่เล่นเสียง (โฟกัสค้างที่ปุ่ม)

- **อาการ:** กดปุ่มบน toolbar (เปิดไฟล์/ซ่อน filler/บันทึก ฯลฯ) แล้วกด
  spacebar เสียงไม่เล่น — ต้องไปคลิกที่คลื่น/บทก่อน Space ถึงเล่น
- **สาเหตุ:** หลังคลิกปุ่ม โฟกัสคีย์บอร์ดค้างที่ปุ่มนั้น; keydown handler มี
  กติกา `if tagName in [INPUT,TEXTAREA,BUTTON] return` — เจอ BUTTON เลย
  ข้าม Space ทั้งหมด (กติกานี้เดิมไว้กันคีย์อื่นตอนพิมพ์)
- **วิธีแก้:** จับ Space ก่อนกติกานั้น — Space เล่น/หยุดเสมอ ยกเว้นโฟกัส
  อยู่ INPUT/TEXTAREA (กำลังพิมพ์แก้คำ); blur ปุ่มที่โฟกัสทิ้งด้วย กัน
  ปุ่มกลืนคีย์ (src/main.ts)
- **กันเกิดซ้ำ:** คีย์ลัดหลัก (Space/เล่น) ต้องทำงานไม่ขึ้นกับว่าโฟกัสอยู่
  ปุ่มไหน — กันเฉพาะช่องพิมพ์ข้อความจริง; ทดสอบ: โฟกัสปุ่มแล้วกด Space ต้องเล่น

## 23–26. บั๊กจาก code review ของ Codex (2026-07-12) — แก้ 4 ตัวแดง

รีวิว read-only โดย Codex (gpt-5.6) เจอ 11 ประเด็น ไม่มี false positive; แก้กลุ่ม
"ความถูกต้อง" 4 ตัว (ที่เหลือเป็น race โอกาสต่ำ/ของเล็ก เก็บทีหลัง):

- **#23 (Codex#1) — ลากขอบ cut แคบลงแล้ว .docx ยังตัดคำที่เสียงกลับมาแล้ว:**
  `updateCutBounds` เก็บ tokenRange เดิมไว้ → `isTokenCut` ยังมองว่าคำนั้นถูกตัด.
  แก้: คำนวณ tokenRange ใหม่จากขอบใหม่ (`tokensInSpan`) — cut แบบไม่มีบท
  (tokenRange=null) คงเป็น null. `src/project.ts`; เทสต์ใน project.test.ts
- **#24 (Codex#9) — long-file cache ติด "prepared" ทั้งที่ peaks หาย:**
  crash ระหว่าง rename WAV กับเขียน peaks → เปิดไฟล์เดิมพังถาวรที่ fetchPeaks.
  แก้: `probe()` เช็คทั้ง WAV **และ** peaks. `backend/app/longfile.py`
- **#25 (Codex#5) — waveform ไฟล์ยาวว่างถาวรหลัง /pcm fail ครั้งเดียว:**
  reject handler ไม่ทำอะไร overlay ยัง active (ws ถูกซ่อน) จอเลยว่าง. แก้:
  reject → `setActive(false)` (คืน ws peaks) **แต่ห้าม requestRender** กัน
  hot-loop fetch. `src/audio/wavedetail.ts`; ยืนยัน pcmCalls ไม่วน
- **#26 (Codex#10) — export ถูก cancel → ทิ้งไฟล์ครึ่งๆ + ทับไฟล์เดิมพัง:**
  เขียนตรงลง out_path. แก้: เขียน `.part` แล้ว `replace()` ตอนจบ (atomic);
  finally ลบ temp ทุกกรณี — ไฟล์เดิมไม่ถูกแตะจนกว่าจะสำเร็จ. `backend/app/render.py`
- **บทเรียน:** ให้ agent ตัวอื่น review แบบ read-only เป็นระยะ — จับ edge case/
  race ที่คนเขียนเองมองข้าม (คู่กับ AGENTS.md ที่ให้กติกาเดียวกัน)

## 27. บั๊กจาก Codex review รอบสอง (2026-07-12) — race/guard/หน่วยความจำ

แก้กลุ่มที่เหลือจากรีวิวเดียวกัน (โอกาสเกิดต่ำแต่เป็น correctness/robustness):

- **(Codex#3) ตัดจาก rough Whisper timestamp ได้:** `cutSelection` บล็อกเมื่อ
  เลือกแบบ token (`selection` ไม่ null) และ `timestamps === "rough"` — แต่ยัง
  อนุญาตลากบน waveform โดยตรง (ผู้ใช้ดูเสียงจริง). `src/main.ts`
- **(Codex#4/#7) ผลงานเก่า apply ผิดไฟล์:** `adoptResult` เช็ค
  `currentPath === audioPath`; `realign` จับ project identity ก่อน await แล้ว
  bail ถ้าเปลี่ยน — กันผลถอด/ตรึงที่เสร็จช้าหลังเปิดไฟล์ใหม่/งานใหม่. `src/main.ts`
- **(Codex#6) onDone ยิงซ้ำ:** `setInterval(async)` overlap ได้ → เพิ่ม flag
  `finished` กัน onDone ทำงานเกินครั้งเดียว. `src/main.ts`
- **(Codex#8) playback ข้าม cut ซ้อนกันไม่ครบ:** วน merge run ของ cut ที่
  ต่อเนื่อง/ซ้อน แล้วกระโดดทีเดียว → preview ตรงกับ export (ที่ merge แล้ว).
  `src/audio/player.ts`
- **(Codex JobStore) RAM โตตามจำนวนงาน:** prune job ที่เสร็จแล้วเมื่อเกิน 40
  (เก็บงานที่ยังรันไว้เสมอ). `backend/app/jobs.py`
- ยังไม่ทำ (latent จริง): #11 ถอด global listener ตอน destroy — view เป็น
  singleton ไม่ remount จึงยังไม่กระทบ; ทำเมื่อมีการ reinit UI

## 28. Codex review รอบสอง (re-review) — ปิดช่องที่การแก้รอบแรกยังไม่ครบ

Codex รีวิวการแก้เอง เจอว่า 3 จุดยังไม่ถึงราก (มันถูก) — แก้เพิ่ม:

- **resize cut ยัง inconsistent (regression ที่เพิ่งสร้าง):** ใช้ "token ทั้งคำ
  ต้องอยู่ใน cut" → ขยับขอบ 10ms เข้าไปในคำ คำหลุด tokenRange กลับเข้า .docx
  ทั้งที่เสียงหายเกือบหมด. แก้: `tokensInSpan` ใช้ **จุดกึ่งกลางคำ** (midpoint)
  — คำถูกตัดถ้ากลางคำอยู่ในช่วง cut; ใช้กติกาเดียวกันทั้ง create + resize.
  เทสต์: nudge 10ms, ขยาย cut, ผ่านกลางคำ. `src/project.ts` + `main.ts`
- **ABA race (A→ไฟล์อื่น→A):** path check ผ่านได้ถ้ากลับมาเปิดไฟล์เดิม. แก้:
  ใช้ `loadGeneration` counter (เพิ่มทุก load/งานใหม่) job จับ gen ตอนเริ่มแล้ว
  bail ถ้า gen ขยับ. `src/main.ts`
- **JobStore prune เฉพาะตอน submit:** ยิง 100 งานพร้อมกัน (active หมด) →
  จบครบแล้วไม่มี submit → ค้าง 100. แก้: prune ใน `finally` ของ `_run` ด้วย
  → หน่วยความจำคงที่จริง. เทสต์ lifecycle จริง. `backend/app/jobs.py`
- **orphan cache recovery พังบน Windows:** ใช้ `rename()` (fail ถ้าปลายทางมี
  อยู่). แก้: `replace()` ทั้ง WAV+peaks + เขียน peaks ลง .part ก่อน replace
  (atomic ทั้งคู่). เทสต์ recover จริง. `backend/app/longfile.py`
- **export temp ชื่อชนกัน:** `<out>.part` คงที่ → เพิ่ม uuid. `render.py`
- เพิ่มเทสต์ตามที่ Codex ชี้: partial-token resize, JobStore lifecycle,
  orphan re-prepare, export replace/error, playback overlap (แยก `skipTarget`
  เป็น pure fn + เทสต์ 6 เคส)
- **บทเรียน:** ให้ reviewer เดิม "รีวิวการแก้" ด้วย — จับ regression ที่คนแก้
  สร้างเอง (whole-token→midpoint) และ over-claim ("RAM flat") ได้

## 29. Codex re-review รอบสาม — ปิด 2 ช่องสุดท้าย

- **create path หลังลากขอบด้วยมือ:** `cutSelection` ใช้ `selection ??
  tokensInSpan` → ถ้าเลือก token แล้วลากขอบ selection บน waveform/spectrogram
  ต่อ `selectionBounds` ขยับแต่ `selection` เดิมค้าง → เสียงตัดตามขอบใหม่ แต่
  เอกสารตัดตาม token เดิม. แก้: `tokenRange = tokensInSpan(start, end)` เสมอ
  (ทิ้ง `selection ??`) — create ใช้ midpoint จาก bounds จริงทุกเส้นทาง
  เท่ากับ resize. `src/main.ts`
- **prune ลบ job ก่อน client เห็นผล:** prune ใน finally อาจลบ job ที่เพิ่งจบ
  ก่อน frontend poll (ทุก ~1s) อ่าน DONE → /jobs/{id} 404 → UI ขึ้น "ตามสถานะ
  งานไม่ได้" ทั้งที่สำเร็จ. แก้: เพิ่ม `finished_at` + `PRUNE_GRACE_SEC=30s`
  — prune เฉพาะ job ที่จบเกิน grace แล้ว (client มีเวลาอ่านผลเสมอ). เทสต์:
  job เพิ่งจบต้องยัง get() ได้แม้เกิน cap. `backend/app/jobs.py`
- **บทเรียน:** "atomic" กับ "prune" ต้องคิดถึง observability ของ client ด้วย
  ไม่ใช่แค่ memory — ของที่ client กำลังรอต้องไม่หายก่อนเขาเห็น

## 30. Codex re-review รอบสี่ — ปิด lifecycle hole ของ grace

- **grace หมดแล้วไม่มีใคร prune:** เพิ่ม grace (ข้อ 29) แก้ 404 ได้ แต่สร้างรู
  ใหม่: ถ้า burst งานจบครบภายใน grace, prune-in-finally ทำอะไรไม่ได้ (ยังใน
  grace) และหลัง grace หมด**ไม่มี event ไหนมาเรียก prune** จนกว่าจะ submit ใหม่
  → store ค้างเกิน cap ได้เรื่อยๆ ตอน idle. แก้: **maintenance thread** (daemon,
  เริ่มตอน submit แรก) เรียก `run_maintenance()` ทุก ~grace วินาที → prune
  งานที่พ้น grace แม้ไม่มี submit ใหม่. เทสต์: submit เกิน cap, รอจบ (ยังเกิน cap
  เพราะ grace), backdate finished_at, เรียก run_maintenance ตรงๆ → ต้องเหลือ
  ≤ cap. `backend/app/jobs.py`
- **บทเรียน:** กลไกที่พึ่ง event (submit/finish) ต้องมี "ตัวกวาดเป็นคาบ" สำรอง
  ด้วย ไม่งั้นสถานะ idle จะค้าง; และเทสต์ที่ตั้ง grace=0 หนีการพิสูจน์ lifecycle
  จริง — ต้องขับ maintenance ด้วยนาฬิกาที่ควบคุมได้

## 31. คลิก spectrogram ของไฟล์สั้นแล้วภาพหายและค้างที่กำลังโหลด

- **อาการ:** ไฟล์สั้นกว่า 20 นาทีซึ่งใช้ `MemorySamples` คลิก spectrogram แล้ว
  ภาพหายและค้างที่ "กำลังโหลด spectrogram..."
- **สาเหตุ:** `MemorySamples.getWindow()` ปัดขอบช่วงทั้งสองด้านด้วย `Math.round`
  ทำให้ `window.startSec` อาจช้ากว่าเวลาต้นช่วงที่ขอ และปลาย window อาจจบก่อน
  เวลาปลายช่วง; coverage check ของ spectrogram จึงไม่ผ่านและ `ensureWindow()`
  ขอ window เดิมวนซ้ำไม่จบ
- **วิธีแก้:** ปัด sample ต้นช่วงลงด้วย `Math.floor` และ sample ปลายช่วงขึ้นด้วย
  `Math.ceil` เพื่อให้ window ครอบช่วงเวลาที่ร้องขอเสมอ (ภายในขอบไฟล์)
- **กันเกิดซ้ำ:** มี unit test ที่ขอช่วงซึ่งขอบเวลาตกกลาง sample แล้วตรวจว่า
  `startSec <= requestedStart` และปลายข้อมูลครอบ `requestedEnd`; provider ใหม่
  ทุกตัวต้องรักษา coverage invariant เดียวกัน

## 32. Windows: backend พังตอนเปิด → "ไม่มีปุ่มติดตั้งโมเดล" (รายงานจากทีม)

- **อาการ:** เครื่อง Windows เปิดแอปแล้วขึ้น "Unhandled exception in script …
  Unable to configure formatter 'default'" และในแอปไม่มีปุ่มติดตั้งโมเดล
  (banner โมเดลมาจาก backend — backend ตายปุ่มจึงไม่โผล่)
- **สาเหตุ:** ตอนซ่อนหน้าต่างดำ console เราตั้ง `console=False` ใน PyInstaller
  spec → บน Windows `sys.stdout`/`sys.stderr` เป็น `None` → uvicorn's
  DefaultFormatter เรียก `sys.stdout.isatty()` → `AttributeError` ตั้งแต่บูต
  (macOS ไม่เจอเพราะ windowed app ยังได้ stream จริง)
- **วิธีแก้:** `serve.py: ensure_streams()` — ถ้า stream เป็น `None` ให้เปิด
  `backend.log` ใน `AUDIOEDIT_DATA_DIR` มาแทน (fallback = devnull) **ก่อน**
  import uvicorn; แถมได้ log ไฟล์ไว้ debug เครื่องทีมครั้งหน้า
- **กันเกิดซ้ำ:** `tests/test_serve.py` จำลอง stdout/stderr = None แล้วตรวจว่า
  `.isatty()`/`.write()` ใช้ได้; กติกา: อะไรก็ตามที่รันใน sidecar แบบ
  `console=False` ห้าม assume ว่ามี stdout/stderr — ทดสอบ Windows build จริง
  ทุกครั้งที่แตะการบูตของ sidecar

## 33. Windows: ถอดเสียงไม่สำเร็จ "cublas64_12.dll is not found" (รายงานจากทีม)

- **อาการ:** เครื่อง Windows กดถอดเสียงแล้วขึ้น
  `RuntimeError: Library cublas64_12.dll is not found or cannot be loaded`
- **สาเหตุ:** `config.device()` ตั้ง default = `"auto"` → CTranslate2
  (เครื่องยนต์ของ faster-whisper) ไปตรวจหาการ์ดจอ NVIDIA แล้วพยายามโหลด
  ไลบรารี CUDA (`cublas64_12.dll`) ซึ่งเครื่องทั่วไปไม่มีและเราก็ไม่ได้แนบไป
  (เราแนบโมเดล int8 สำหรับ CPU) — macOS ไม่เจอเพราะไม่มี CUDA path
- **วิธีแก้:** เปลี่ยน default เป็น `"cpu"` (`config.py`); ถ้าใครตั้ง
  `AUDIOEDIT_DEVICE=cuda` เองแต่โหลด CUDA ไม่ได้ `asr.py` จะ fallback มา
  CPU (`compute_type=int8`) อัตโนมัติแทนที่จะพัง; aligner (torch) อยู่ CPU
  อยู่แล้ว (ไม่มี `.to(cuda)`)
- **กันเกิดซ้ำ:** `test_config.py::test_device_defaults_to_cpu` ล็อกค่า default;
  กติกา: แอปนี้ต้องรันได้บนเครื่องไม่มี GPU เสมอ — GPU เป็น opt-in ผ่าน env
  เท่านั้น ห้ามตั้ง `device="auto"` เป็น default อีก

## 34. Windows: คลื่นเสียงล้นกรอบดำ ทับลงไปในแถบ scrollbar (รายงานจากทีม)

- **อาการ:** บน Windows ยอดคลื่นด้านล่างยื่นทะลุพื้นหลังดำของตัวเอง ลงไป
  เกยแถบสีเทาข้างล่าง (มีภาพจากทีม) — บน MacBook ไม่เป็น
- **สาเหตุ:** `.wave-detail` (overlay คลื่น true-PCM ของ Phase 9e) ตั้ง
  `inset: 0; height: 100%` = สูงเท่า `#waves` ทั้งก้อน แต่ `#waves` มีทั้ง
  canvas คลื่น (สูง 120px) **และ** scroll container ของ wavesurfer ซึ่งงอก
  scrollbar แนวนอนตอนซูมจนล้น — scrollbar บน Windows กินพื้นที่ layout จริง
  (วัดได้ 15px) ส่วน macOS เป็น overlay scrollbar กินพื้นที่ 0px
  → บน Windows overlay สูง 136.2px แทนที่จะเป็น 120px วาดคลื่นโดยยึด
  กึ่งกลางที่ 68.1 แทนที่จะเป็น 60 = เพี้ยนลงล่าง 8.1px
- **วิธีแก้:** export ค่า `WAVE_HEIGHT` จาก `player.ts` (ค่าเดียวกับที่ส่งให้
  `WaveSurfer.create({ height })`), `main.ts` เซ็ตเป็น CSS var
  `--wave-height` บน `#waves`, และ `.wave-detail` ใช้
  `top: 0; height: var(--wave-height)` แทน `inset: 0` — overlay จึงผูกกับ
  ความสูงคลื่นจริง ไม่ใช่ความสูงคอนเทนเนอร์
- **กันเกิดซ้ำ:** ความสูงคลื่นมีแหล่งความจริงเดียวคือ `WAVE_HEIGHT` ห้าม
  hardcode 120 ซ้ำที่อื่น; กติกา: **ห้ามใช้ `inset: 0` / `height: 100%` กับ
  overlay ที่ทับ element ซึ่งมี scrollbar ของตัวเอง** เพราะ Mac จะไม่มีวัน
  แสดงบั๊กนี้ให้เห็น — ทดสอบบน Windows เสมอเมื่อแตะ overlay ของ waveform

## 35. Windows: หมุนล้อเมาส์แล้วไม่ซูม (ฟีเจอร์กลายเป็น Mac-only โดยไม่ตั้งใจ)

- **อาการ:** บน MacBook ปัด trackpad แล้ว waveform ซูมเข้า/ออกได้เลย แต่บน
  Windows หมุนล้อเมาส์แล้วไม่มีอะไรเกิดขึ้น (รายงานจากทีม)
- **สาเหตุ:** handler `wheel` ใน `main.ts` ขึ้นต้นด้วย
  `if (!we.ctrlKey && !we.metaKey) return;` คือบังคับให้กด Ctrl/Cmd ค้าง
  บน macOS ท่า **pinch-zoom บน trackpad** เบราว์เซอร์จะสังเคราะห์ wheel event
  ที่มี `ctrlKey = true` ให้เอง → ผ่าน guard นี้พอดีโดยผู้ใช้ไม่ต้องกดอะไร
  ทำให้ดูเหมือนฟีเจอร์ทำงาน ส่วนล้อเมาส์บน Windows ไม่ส่ง modifier มาเลย
  → โดน guard ตีตกทุกครั้ง
- **วิธีแก้:** ล้อเปล่าๆ = ซูม (ตาม muscle memory ของ Sound Forge);
  Ctrl/Cmd+ล้อ ยังซูมเหมือนเดิม (ท่า pinch ของ Mac จึงไม่พัง);
  Shift+ล้อ และการปัดแนวนอน (`deltaX`) = เลื่อนคลื่นซ้าย/ขวา ผ่าน
  `player.panBy()`; **Alt+ล้อ = ปล่อยให้ panel เลื่อนแนวตั้งตามปกติ**
- **กันเกิดซ้ำ:** ต้องมี Alt escape hatch เสมอ — `.waves-scroll` สูงรวม
  ~325px (คลื่น + spectrogram) ซึ่งล้น panel บนหน้าต่างความสูงปกติ
  (วัดได้ล้น 63px ที่หน้าต่าง 694px) ถ้ายึดล้อไปใช้ซูมทั้งหมดโดยไม่เหลือทาง
  spectrogram จะถูกดันตกจอโดยไม่มีทางเลื่อนกลับ;
  กติกา: **modifier ที่ macOS สังเคราะห์เอง (ctrlKey จาก pinch) ห้ามใช้เป็น
  เงื่อนไขเดียวของฟีเจอร์** ไม่งั้นฟีเจอร์จะเงียบหายไปบน Windows

## 36. "ตรวจพลังเสียง" ฟ้องผิดว่าไฟล์เปลี่ยน ทั้งที่ export ตรงเป๊ะ (เจอตอนเทสต์ไฟล์จริง)

- **อาการ:** เครื่องมือตรวจพลังเสียงตัวใหม่ ตัดสินว่า export ของทีม
  "พลังเสียงเปลี่ยน" ทั้งที่ RMS ต่างกันแค่ **-0.001 dB** — ตัวที่เกินเกณฑ์คือ
  peak ที่ต่างไป **-0.68 dB**
- **สาเหตุ:** ไฟล์ต้นฉบับเป็น **MP3** และ peak ของมันคือ **+0.68 dBFS คือ
  เกินเต็มสเกล** — เรื่องปกติของ codec แบบ lossy (decoder สร้างคลื่นกลับจาก
  โดเมนความถี่ ค่าที่ได้ล้นกว่าเดิมได้) แต่ **integer PCM เก็บค่าเกิน 1.0
  ไม่ได้** ทุก export ที่ซื่อสัตย์จึงต้อง clamp ลงมาที่ 0 dBFS
  `compare()` เอา peak ของ export ไปลบกับ peak **ดิบ** ของต้นฉบับ จึงได้ผลต่าง
  -0.68 dB เสมอเมื่อต้นฉบับเป็น mp3 = ตีตราไฟล์ที่ถูกต้องว่าผิด
- **วิธีแก้:** เทียบกับ `min(source.peak, 1.0)` คือ "เพดานที่ฟอร์แมตปลายทาง
  ไปถึงได้จริง" แทน peak ดิบ; เพิ่ม `source_over_full_scale` +
  `source_peak_dbfs_raw` ใน response และให้ UI อธิบายให้ผู้ใช้อ่านออกว่า
  "ต้นฉบับพีคเกินเต็มสเกล จึงถูกจำกัดที่ 0 dBFS ตามข้อจำกัดของ PCM
  ไม่ใช่การบีบอัด" — ผลหลังแก้กับไฟล์จริง: RMS -0.0009 dB, peak -0.0003 dB
- **กันเกิดซ้ำ:** `test_over_full_scale_source_is_not_reported_as_changed`
  ล็อกเคสนี้ด้วยตัวเลขจากไฟล์ของทีมจริง (+0.68 dBFS) และ
  `test_a_genuinely_quieter_export_is_still_caught_when_source_clips`
  กันไม่ให้ข้อยกเว้นนี้กลายเป็นใบผ่านทุกอย่าง;
  กติกา: **เครื่องมือตรวจสอบเสียงต้องทดสอบกับไฟล์จริงเสมอ** — สัญญาณ
  สังเคราะห์ (sine 0.5) ไม่มีวันมี inter-sample over จึงไม่เจอบั๊กประเภทนี้

## 37. Alt+ล้อเมาส์ ไม่เลื่อนอะไรเลย (รายงานจากผู้ใช้)

- **อาการ:** คู่มือบอกว่า Alt+ล้อ = เลื่อนขึ้นลงดู spectrogram แต่กดแล้วไม่มี
  อะไรเกิดขึ้น
- **สาเหตุ:** ตอนแก้ #35 (ล้อเปล่า = ซูม) ผมกัน Alt ไว้ด้วย `if (we.altKey)
  return;` โดย**เข้าใจผิดว่า** ถ้าไม่เรียก `preventDefault()` เบราว์เซอร์จะ
  เลื่อน panel ให้เอง — **ไม่จริง** Chromium ไม่ถือว่า Alt+ล้อ เป็นท่าเลื่อน
  ตั้งแต่แรก จึงไม่เลื่อนอะไรเลย กลายเป็นว่า Alt ไปปิดทั้งการซูมและการเลื่อน
- **วิธีแก้:** เลื่อนเองในโค้ด — `we.preventDefault()` แล้ว
  `wavesScroll.scrollTop += we.deltaY`
- **กันเกิดซ้ำ:** กติกา: **การ "ปล่อยให้เบราว์เซอร์จัดการเอง" ต้องพิสูจน์ว่า
  เบราว์เซอร์ทำจริง** ไม่ใช่สมมติ — โดยเฉพาะ modifier ที่ไม่ใช่ท่ามาตรฐาน;
  ทดสอบแล้วด้วยไฟล์จริง (panel 314px ใน 340px): Alt+ล้อลง 0→26, ล้อขึ้น →0,
  และล้อเปล่ายังซูมโดยไม่เลื่อน

## 38. จอกระพริบดำทุกครั้งที่หัวอ่านกระโดด (รายงานจากผู้ใช้ ตอนกด Ctrl+K)

- **อาการ:** กด Ctrl+K แล้ว "หน้าจอเหมือนดับ หรือกระพริบชั่วเสี้ยววินาที"
- **สาเหตุ:** ไม่ใช่เรื่องของ Ctrl+K — เกิดกับ**การเปลี่ยนมุมมองทุกครั้ง**ใน
  โหมดไฟล์ยาว (seek, คลิกคำ, preview) Ctrl+K แค่กระโดด 2 ครั้งติดจึงเห็นชัด
  ทั้งสอง canvas ทิ้งภาพเดิมระหว่างรอ PCM ชุดใหม่จาก backend:
  `spectrogram.ts` คำนวณ base ใหม่ได้ null แล้วตกไปถมดำ + ขึ้น
  "กำลังโหลด…"; `wavedetail.ts` เรียก `clearRect()` **ก่อน** เช็คว่าข้อมูล
  พร้อมหรือยัง แล้ว return ทิ้งไว้ว่าง
- **วิธีแก้:** ถือภาพเดิมไว้จนกว่าข้อมูลจริงจะมา — spectrogram วาดภาพเดิมที่
  **35% ใต้ข้อความกำลังโหลด** (ภาพเดิมคือเสียงคนละช่วง ห้ามให้ดูเหมือนของจริง
  เพราะผู้ใช้อาจวางจุดตัดจากภาพผิด) และ `setProvider()` ยังทิ้งภาพทันทีเมื่อ
  เปลี่ยนไฟล์
- **กันเกิดซ้ำ:** แยกการตัดสินใจเป็นฟังก์ชันบริสุทธิ์ `planFrame()` +
  เทสต์ 4 เคส (มีภาพใหม่ / ไม่มีแต่มีภาพเก่า / ไม่มีทั้งคู่ / ห้ามเลือกภาพเก่า
  ทับภาพใหม่); กติกา: **ห้ามล้าง canvas ก่อนที่จะมีของมาวาดแทน**

## 39. ปุ่มลัด Ctrl ทุกตัวไม่ทำงานเมื่ออยู่แป้นภาษาไทย (รายงานจากผู้ใช้)

- **อาการ:** กด Ctrl+Z แล้วไม่ย้อน แต่**กดปุ่ม ↺ ใน toolbar ย้อนได้ปกติ** —
  แปลว่าโมเดล undo ไม่ได้เสีย event คีย์บอร์ดต่างหากที่ไม่ถึงตัวจัดการ
- **สาเหตุ:** ทุกปุ่มลัดเช็คด้วย `e.key.toLowerCase() === "z"` แต่ `e.key` คือ
  **ตัวอักษรที่แป้นพิมพ์ผลิตออกมา** พอสลับเป็นแป้นไทย ปุ่ม Z รายงานว่า
  **"ผ"** เงื่อนไขจึงไม่ผ่าน — **ปุ่มลัด Ctrl ทั้งหมด (S/F/L/K/Z/A) และปุ่ม M
  พังพร้อมกันทันทีที่ผู้ใช้สลับไปพิมพ์ไทย** ซึ่งเป็นสถานะปกติของแอปที่ทำมา
  เพื่อแก้บทภาษาไทยโดยเฉพาะ; ที่บางครั้งใช้ได้เพราะตอนนั้นอยู่แป้นอังกฤษพอดี
  จึงดูเหมือนบั๊กสุ่ม
- **วิธีแก้:** เพิ่ม `isChord()` / `isPlainKey()` ที่เทียบ **`e.code`** ซึ่งเป็น
  **ปุ่มทางกายภาพ** (`"KeyZ"`) ไม่ขึ้นกับแป้นพิมพ์ และคง `e.key` ไว้เป็น
  ตัวสำรองสำหรับแป้น Latin ที่สลับตำแหน่งปุ่ม (Dvorak/AZERTY);
  แถมแก้ให้ **Ctrl+Z ในช่องพิมพ์ไม่ไปแย่ง undo ของข้อความ** อีกต่อไป
- **กันเกิดซ้ำ:** ทดสอบกับ build จริงทั้งสองแป้น — ไทย: Ctrl+Z/K/S/F/L และ M
  ทำงานครบ, อังกฤษ: ครบเหมือนกัน, และ Ctrl+Z ในช่องค้นหาไม่ถูกดักแล้ว;
  **กติกา: ห้ามใช้ `e.key` ตัดสินปุ่มลัดอีก — แอปนี้ผู้ใช้พิมพ์ไทยเป็นหลัก
  ต้องใช้ `e.code` เสมอ**

## 40. กด Play แล้ว spectrogram หายไป ค้างที่ "กำลังโหลด" ตลอด (รายงานจากผู้ใช้)

- **อาการ:** กด Play คลื่นเสียงยังอยู่ แต่ spectrogram หายไปทั้งแถบ ขึ้นว่า
  "กำลังโหลด spectrogram…" ค้างไว้ตลอดการเล่น
- **สาเหตุ:** `ensureWindow()` ขอ PCM มา**เท่ากับช่วงที่มองเห็นพอดีเป๊ะ**
  (บวก FFT margin แค่ระดับมิลลิวินาที) ตอนเล่นเสียง autoScroll เลื่อนมุมมอง
  ตลอดเวลา → หลุดขอบหน้าต่างที่ดึงมาตั้งแต่เฟรมถัดไป → เริ่มดึงใหม่ →
  `fetchGen` ทำให้คำขอเดิมเป็นโมฆะ → **คำขอทุกอันถูกยกเลิกก่อนจะวาดเสร็จ**
  ภาพจึงไม่มีวันขึ้นเลยตลอดการเล่น (ต่างจาก #38 ที่แค่กระพริบตอนกระโดด —
  อันนี้คือไม่ขึ้นเลย)
- **วิธีแก้:** `windowRangeFor()` ดึงหน้าต่าง**กว้างกว่าที่มอง** โดยใช้ headroom
  ที่ provider ให้ได้ (สูงสุดข้างละ 1 ช่วงมอง) แต่ไม่เกิน `maxWindowSec` และ
  ไม่เลยขอบไฟล์ — ซื้อเวลาเลื่อนได้**หลายวินาที**ต่อการดึงหนึ่งครั้ง แทนที่จะ
  เป็นเสี้ยววินาที; ใช้กับ `wavedetail.ts` ด้วยเพราะมีปัญหาเดียวกันเป๊ะ
- **กันเกิดซ้ำ:** `windowRangeFor()` เป็นฟังก์ชันบริสุทธิ์ + เทสต์ 9 ตัว
  โดยตัวสำคัญคือ **"หน้าต่างที่ดึงตอน t ต้องยังคลุมมุมมองอีก 5 วินาทีถัดไป"**
  ซึ่งเป็นตัวเลขที่ตัดสินว่าภาพจะอยู่รอดระหว่างเล่นหรือไม่;
  กติกา: **อย่าดึงข้อมูลเท่ากับที่แสดงพอดี ถ้าสิ่งที่แสดงมันเลื่อนได้เอง**

## 41. คลิกคำในบทแล้วคลื่นเด้งไปชิดซ้ายสุด ไม่อยู่ตรงกลาง (รายงานจากผู้ใช้)

- **อาการ:** กดเลือกคำในบท คลื่นเด้งไปที่คำนั้นจริง แต่ตำแหน่งไป**ชิดขอบซ้าย
  มากๆ** แทนที่จะอยู่กลางจอ ทำให้มองไม่เห็นเสียงที่อยู่ "ก่อนหน้า" คำนั้น
- **สาเหตุ:** `seekTo()` เรียก `setScrollTime(seconds - 1)` คือวางเวลาที่
  `target - 1` ไว้ที่ **ขอบซ้าย** ตั้งใจให้มีเสียงนำ 1 วินาที แต่ช่วงที่มองเห็น
  กว้างราว 60 วินาทีที่ซูมปกติ → 1 วินาทีคือ **ประมาณ 2% จากซ้าย** = ชิดซ้าย
  และเห็นแต่เสียงที่อยู่ "หลัง" คำนั้นเต็มจอ ซึ่งตรงข้ามกับสิ่งที่ต้องใช้ตอน
  ตัดสินใจว่าจะตัดตรงไหน (ต้องเห็นทั้งก่อนและหลัง)
- **วิธีแก้:** `scrollTimeFor()` วางเป้าหมายไว้ **กึ่งกลาง** —
  `target - visibleSpan/2` โดย clamp ทั้งสองด้านไม่ให้เลื่อนเลยขอบไฟล์
  (ใกล้ต้นไฟล์เป้าหมายจะอยู่ซ้ายของกลาง ใกล้ท้ายไฟล์จะอยู่ขวาของกลาง
  แต่ยังอยู่ในจอเสมอ)
- **กันเกิดซ้ำ:** ฟังก์ชันบริสุทธิ์ + เทสต์ 7 ตัว รวมกรณีขอบไฟล์ ไฟล์สั้นกว่า
  จอ และ `clientWidth = 0` ตอน layout ยังไม่เสร็จ (ต้องไม่ได้ NaN);
  ทดสอบกับไฟล์จริง 50 นาทีด้วย ได้ **0.500 ทุกระดับซูม**

## 42. ปุ่ม ±1ms/±10ms "ต้องกดหลายครั้งถึงจะไป" (รายงานจากผู้ใช้)

- **อาการ:** กดปุ่มเก็บละเอียดขอบตัดแล้วเหมือนไม่ติด ต้องกดหลายครั้งกว่าจะเห็น
  อะไรขยับ
- **สาเหตุ:** **ปุ่มไม่ได้เสีย** — วัดแล้วขยับตรงเป๊ะทุกครั้งที่กด (+1ms →
  +0.001s, +10ms → +0.010s, กด 5 ครั้ง → +0.005s) ปัญหาคือ**มองไม่เห็น**:
  ที่ซูมเริ่มต้น 20 px/s หนึ่งมิลลิวินาที = **0.02 พิกเซล** ต้องกด **50 ครั้ง**
  ภาพถึงจะขยับ 1 พิกเซล ตัวเลขในแถบเปลี่ยนทุกครั้ง แต่แถบฟ้าไม่ขยับ
  ผู้ใช้จึงสรุปว่าปุ่มไม่ทำงาน

  | ซูม | 1ms = กี่พิกเซล | กดกี่ครั้งถึงขยับ 1 พิกเซล |
  |---|---|---|
  | 0 (เริ่มต้น) | 0.02 | **50** |
  | 50 | 0.98 | 2 |
  | 80 | 8.4 | 1 |

- **วิธีแก้:** เพิ่มคำเตือนในแถบเก็บละเอียดเมื่อก้าวเดินเล็กกว่า 1 พิกเซล —
  บอกตรงๆ ว่า "ซูมเข้าเพื่อเห็นการขยับ — ตอนนี้ 10ms ≈ 0.20 พิกเซล
  (ตัวเลขเปลี่ยนทุกครั้งที่กด แต่ภาพยังไม่ขยับ)" คำเตือนหายเองเมื่อซูมพอ
  **ไม่แก้ด้วยการเพิ่มขนาดก้าว** เพราะความละเอียดคือเหตุผลที่ปุ่มนี้มีอยู่
- **กันเกิดซ้ำ:** `fineStepVisibility()` เป็นฟังก์ชันบริสุทธิ์ + เทสต์ 5 ตัว
  (รวมกรณี pxPerSec = 0 ตอนยังไม่รู้ซูม ต้องไม่หารด้วยศูนย์);
  กติกา: **ถ้าปุ่มทำงานแต่ผลเล็กเกินกว่าจะเห็น ต้องบอกผู้ใช้ ไม่ใช่ปล่อยให้
  เดาว่าพัง**

## 43. Ctrl+K เด้งไกลเกินไป รอยตัดหลุดออกนอกจอตอนซูมเข้า (รายงานจากผู้ใช้)

- **อาการ:** กด Ctrl+K แล้วหน้าจอเด้งไปไกล ไม่เห็นรอยตัดที่กำลังจะตรวจ
- **สาเหตุ (2 ชั้น):**
  1. `previewCut()` เรียก `seekTo(start - preRoll)` และหลังแก้ #41 ให้ seek
     วางกึ่งกลาง มันจึงวาง**จุดที่อยู่ก่อนรอยตัด 1.5 วินาที**ไว้กลางจอ —
     รอยตัดที่ผู้ใช้สนใจไปอยู่ค่อนขวา
  2. `preRoll` ตั้งตายตัวที่ 1.5 วินาที แต่ช่วงที่มองเห็น**แคบลงตามซูม** —
     ที่ 980 px/s จอกว้างแค่ **1.3 วินาที** ซึ่ง**สั้นกว่าระยะนำ** จึงเป็นไป
     ไม่ได้เลยที่จะเห็นทั้งหัวอ่านและรอยตัดพร้อมกัน (วัดได้ รอยตัดอยู่ที่
     ตำแหน่ง 1.497 ของจอ = หลุดออกไปทางขวา)
- **วิธีแก้:** วาง **รอยตัด** ไว้กลางจอ (ไม่ใช่จุดเริ่มเล่น) และให้
  `previewRollFor()` ย่อระยะนำตามช่วงที่มองเห็น (35% ของจอ, ต่ำสุด 0.25 วิ,
  สูงสุด 1.5 วิ) — วัดผลจริง: ซูม 50 จาก **1.497 → 0.500** (กลางพอดี)
  ซูม 20 คงที่ 0.500 เท่าเดิม
- **ข้อจำกัดที่เหลือ:** ที่ซูมลึกมาก (0.27 วิ/จอ) `ws.play()` เปิด autoScroll
  ซึ่งเลื่อนจอตามหัวอ่านทันทีและทับการจัดวางของเรา — **ตรึงจุดไว้กลางจอ
  ระหว่างเล่นไม่ได้** ที่ความละเอียดนั้น (เสียง 1 วินาที = เกือบ 4 หน้าจอ)
  ยอมรับข้อจำกัดนี้ เพราะซูมระดับนั้นมีไว้วางจุดตัดด้วยตา ไม่ได้ใช้ฟัง
- **กันเกิดซ้ำ:** `previewRollFor()` เป็นฟังก์ชันบริสุทธิ์ + เทสต์ 5 ตัว
  รวมกรณี "ระยะนำต้องสั้นกว่าความกว้างจอเสมอ" และกรณียังไม่รู้ซูม

## 44. v1.3.2 พัง 3 อาการพร้อมกัน: เคอร์เซอร์ค้างกลางจอ คลื่นไหลถอยหลัง spectrogram ค้างโหลด

- **อาการ (รายงานจากผู้ใช้ หลังลง v1.3.2):**
  1. เคอร์เซอร์ที่ปกติไหลตอนเล่น **ค้างอยู่กลางจอ ไม่ขยับเลย**
  2. แถบสีฟ้าและคลื่น **เลื่อนถอยหลัง**ผ่านเคอร์เซอร์แทน
  3. spectrogram **ค้างที่ "กำลังโหลด" ตลอดเวลา**
- **สาเหตุ — ทั้งสามมาจากต้นตอเดียว:** wavesurfer ตั้ง `autoCenter: true`
  เป็นค่าเริ่มต้น และ renderer ทำงานแบบนี้:

      const center = progressWidth - scrollLeft - middle;
      if (isPlaying && autoCenter && center > 0) { ...ตรึงเคอร์เซอร์กลางจอ... }

  เงื่อนไข `center > 0` = ทำงานเฉพาะเมื่อเคอร์เซอร์**เลยกึ่งกลางไปแล้ว**
  เดิม `seekTo()` วางเคอร์เซอร์ไว้ใกล้ขอบซ้าย (`seconds - 1`) มันจึงมีพื้นที่
  ไหลเกือบเต็มจอก่อนจะถูกตรึง = ที่ผู้ใช้เรียกว่า "ไหล"
  พอ #41 เปลี่ยนให้ seek วางกึ่งกลาง เคอร์เซอร์**มาถึงกลางจอตั้งแต่เฟรมแรก**
  จึงถูกตรึงทันที และเพราะจอต้องเลื่อนตามเสียงทุกเฟรม **viewport เปลี่ยน
  ตลอดเวลา** → spectrogram สั่งคำนวณใหม่ไม่หยุด → ไม่มีวันวาดเสร็จ (อาการ 3)
- **วิธีแก้:** ตั้ง `autoCenter: false` — เคอร์เซอร์ไหลข้ามจอจนถึงขอบแล้วค่อย
  เลื่อนทีเดียว แบบเดียวกับโปรแกรมตัดต่อเสียงทั่วไป **โดยการวางกึ่งกลาง
  ตอนคลิกคำ (#41) ยังอยู่ครบ** เพราะนั่นเป็น `setScrollTime()` ของเราเอง
  ตอนหยุดเล่น ไม่เกี่ยวกับ autoCenter
- **กันเกิดซ้ำ:** วัดกับไฟล์จริง — หลังแก้: เคอร์เซอร์เดินจาก 0.500 → 0.569
  ระหว่างเล่น, คลิกคำยังได้ 0.500, และ **viewport เปลี่ยน 0 ครั้งจาก 50
  ตัวอย่างใน 5 วินาที** (เดิมเปลี่ยนแทบทุกเฟรม);
  กติกา: **การเปลี่ยนตำแหน่ง scroll กระทบพฤติกรรมตอนเล่นด้วยเสมอ —
  แก้เรื่องตอนหยุด ต้องทดสอบตอนเล่นด้วยทุกครั้ง**

## 45. แก้ตัวหนังสือไม่ได้เลย — กดดินสอก็เงียบ ดับเบิลคลิกก็เงียบ (รายงานจากผู้ใช้)

- **อาการ:** กดปุ่มดินสอ ✎ ไม่มีอะไรเกิดขึ้น ดับเบิลคลิกคำก็ไม่มีอะไรเกิดขึ้น
  แก้ตัวหนังสือไม่ได้เลยทั้งสองทาง
- **สาเหตุ:** ทั้ง `startEdit()` และ `startSegmentEdit()` ขึ้นต้นด้วย
  `if (!this.project || this.editingActive()) return;` — **ถ้ามีกล่องแก้ไข
  เปิดค้างอยู่ที่ไหนสักแห่ง คำขอแก้ไขทุกครั้งจะถูกทิ้งเงียบๆ**
  และกล่องที่ค้างอยู่**มักมองไม่เห็น**: คิว Tab (ไล่ตรวจคำขีดแดง) เปิดกล่อง
  ตรงตำแหน่งที่คิวชี้ ซึ่งอาจเลื่อนพ้นจอไปแล้ว ส่วน Escape ปิดได้เฉพาะตอนที่
  โฟกัสยังอยู่ในกล่องนั้น ผู้ใช้จึงเห็นแค่ "บทที่แก้ไม่ได้" โดยไม่มีคำอธิบาย
  (ญาติกับ FIXES #20 ซึ่งแก้เรื่องธงค้างเมื่อ DOM หายไป — แต่เคสนี้ DOM
  **ยังอยู่** ตัวรีเซ็ตอัตโนมัติจึงไม่ทำงาน)
- **วิธีแก้:** `finishOpenEditor()` — คำขอแก้ไขที่ผู้ใช้ตั้งใจกด **ต้องชนะเสมอ**
  ถ้ามีกล่องเปิดอยู่ให้**ปิดพร้อมบันทึกสิ่งที่พิมพ์ไว้** แล้วเปิดกล่องใหม่
  เก็บตัวปิดไว้ใน `closeEditor` และล้างทิ้งใน `render()` ด้วย เพราะ closure
  จับ DOM ที่กำลังจะถูกทำลาย
- **กันเกิดซ้ำ:** ทดสอบในเบราว์เซอร์กับบทจริง 400 คำ + เสียงจริง —
  กด Tab เปิดกล่องค้างไว้ แล้วกดดินสอ (เดิมเงียบ) ได้ผล และดับเบิลคลิกคำ
  (เดิมเงียบ) ได้ผล โดย**มีกล่องเปิดอยู่ 1 กล่องเสมอ** ไม่ซ้อนกัน;
  กติกา: **การกดที่ผู้ใช้ตั้งใจ ห้ามเงียบ** — ถ้าทำไม่ได้ต้องบอกเหตุผล
  ถ้าทำได้ต้องทำ ห้าม return เฉยๆ

## 46. spectrogram เบลอตอนซูมเข้าลึก (รายงานจากผู้ใช้)

- **อาการ:** "ตอนนี้ spectrogram เวลาขยายแล้วไม่ชัดเหมือนเวอร์ชั่นก่อนไหม
  หรือฉันคิดไปเอง"
- **สาเหตุ:** `chooseFftSize()` เลือกขนาดหน้าต่างวิเคราะห์จาก hop อย่างเดียว
  และมีค่าต่ำสุดที่ 256 พอซูมเข้าจน hop เล็ก ขนาดจึงตกไปที่ 256 เสมอ
  ซึ่งที่ 44.1kHz คือ **172 Hz ต่อแถบ** — เสียงชายพื้นฐาน ~120 Hz กับ
  หญิง ~220 Hz ตกอยู่ในแถบเดียวกัน ฮาร์โมนิกหายหมด
  **ไม่ใช่การถอยหลัง**: โค้ดตัวนี้เหมือนกันทุกตัวอักษรตั้งแต่ v1.0.0
  (ตรวจย้อนทั้ง 6 commit) สิ่งที่เปลี่ยนคือ v1.2–1.3 เพิ่ม Ctrl+K,
  ซูมละเอียด, ล็อกช่วง ทำให้ผู้ใช้**ไปอยู่ที่ระดับซูมลึกเป็นปกติ**
  ซึ่งเมื่อก่อนแทบไม่เคยเข้าไป ของเบลอมาตลอด แต่เพิ่งได้เห็น
- **วิธีแก้:** ยกค่าต่ำสุดเป็น `MIN_FFT_SIZE = 1024` → 43 Hz ต่อแถบ
  (ละเอียดขึ้น 4 เท่า) แลกกับการคำนวณราว 5 เท่าบนเฟรมที่ใช้เวลาไม่กี่มิลลิวินาที
- **กันเกิดซ้ำ:** เทสต์ยืนยันคุณสมบัติเป็น **Hz ไม่ใช่จำนวนแท็ป** —
  "ที่ระดับซูมลึกสุดต้องได้ความละเอียดดีกว่า 50 Hz" ซึ่งยังจริงอยู่แม้
  เปลี่ยนวิธีเลือกขนาดในอนาคต

## 47. ภาพเก่าของ spectrogram ที่ควรจาง กลับสว่างเต็มที่ (เจอตอนตรวจ #46)

- **อาการ:** ไม่มีใครรายงาน — เจอระหว่างอ่านโค้ดเพื่อตอบเรื่องความชัด
  ตัวกันไม่ให้ตัดผิดตำแหน่ง**ไม่ทำงานมาตลอด**
- **สาเหตุ:** FIXES #40 ตั้งใจให้ตอนภาพใหม่ยังโหลดไม่เสร็จ ให้แสดงภาพเก่า
  **แบบจาง** เพื่อไม่ให้ผู้ใช้เผลอวางรอยตัดโดยดูภาพจากตำแหน่งอื่นของไฟล์
  โค้ดเขียนว่า `ctx.globalAlpha = 0.35; ctx.putImageData(...)`
  แต่ **`putImageData()` ไม่สนใจ `globalAlpha`** — มันเขียนพิกเซลลง bitmap
  ตรงๆ ข้ามระบบผสมภาพทั้งหมด (รวมถึง globalCompositeOperation, transform,
  clip) ภาพเก่าจึงแสดง**เต็มความสว่าง** ต่างจากภาพจริงแค่ตัวหนังสือ
  "กำลังโหลด" ที่ทับอยู่
- **วิธีแก้:** `drawDimmed()` — วาด ImageData ลง canvas ชั่วคราวก่อน
  แล้วค่อย `drawImage()` ซึ่งอยู่ในระบบผสมภาพจริงและรับ globalAlpha
- **กันเกิดซ้ำ:** กติกา — **ถ้าตั้งใจให้ภาพจาง/โปร่ง ห้ามใช้ putImageData**
  ทุก effect ของ canvas 2D (alpha, blend, transform, clip) ใช้ไม่ได้กับมัน

## 48. ตัววัดพลังเสียงใช้คนละมาตรฐานกับที่องค์กรใช้ตรวจ (คำถามจากผู้ใช้)

- **อาการ:** "ที่ทำงานเขาใช้ izotope กับ spectolayers มาวัดพลังเสียงเฉยๆ
  ต้องถามว่าใช้มาตรฐานเดียวกันวัดเทียบไหม"
- **สาเหตุ:** เราวัด RMS ธรรมดา + sample peak ส่วน RX/SpectraLayers แสดง
  **LUFS (ITU-R BS.1770-4, ถ่วงน้ำหนัก K)** และ **true peak (oversample)**
  คนละนิยาม ตัวเลขจึงไม่มีวันตรงกัน ทีมไม่มีทางแยกออกว่าที่ต่างเพราะ
  หน่วยคนละแบบ หรือเพราะโปรแกรมเราทำเสียงเพี้ยนจริง
- **กับดักที่อันตรายกว่า:** ค่าฝั่ง "ต้นฉบับ" ของเราวัด**เฉพาะช่วงที่เก็บไว้**
  (ส่ง EDL เข้า `measure()`) เพื่อให้เทียบกับไฟล์ export ได้ ถ้าทีมทำแบบ
  ธรรมชาติที่สุด — เปิดต้นฉบับทั้งไฟล์ใน RX เทียบกับไฟล์ที่ตัดแล้ว —
  **ตัวเลขจะไม่ตรงเสมอ เพราะเนื้อหาถูกตัดออก ไม่ใช่เพราะบีบอัด**
  แล้วจะสรุปว่าโปรแกรมเราพัง
- **วิธีแก้:** `loudness.py` เพิ่ม LUFS + true peak ตามมาตรฐานเดียวกับ RX;
  `compare()` คืน `source_measured_through_cuts` และหน้าจอผลลัพธ์อธิบาย
  วิธีเทียบใน RX ที่ให้ผลถูกต้อง (เลือก**ช่วงที่ไม่ได้ถูกตัด**ในทั้งสองไฟล์)
- **กันเกิดซ้ำ:** เทสต์เป็นชุด **EBU Tech 3341** ของจริง ไม่ใช่ "ดูแล้วน่าจะถูก" —
  ถ้าเลขเราไม่ตรงมาตรฐานที่โปรแกรมอื่นทำตาม ทีมกำลังเทียบคนละสิ่งโดยไม่รู้ตัว
  (case 3 ความยาว 10/60/10 วินาที **สำคัญ** — ถ้าใช้ 10/10/10 คำตอบที่ถูกคือ
  -27.3 ไม่ใช่ -23 ผมเขียนเทสต์ผิดรอบแรกและเกือบไปแก้โค้ดที่ถูกอยู่แล้ว)

## 49. วัดความดังครั้งแรกใช้เวลา 11.5 นาที (เจอตอนทำ #48)

- **อาการ:** `measure()` บนไฟล์จริง 50 นาที ใช้ 688 วินาที — ปุ่มที่ไม่มีใครกดสองครั้ง
- **สาเหตุ (สามชั้น ต้องวัดถึงจะเจอ):**
  1. docstring ผมเขียนว่าใช้ FFT แต่โค้ดใช้ `np.convolve` ซึ่งคูณตรงๆ
     O(ตัวอย่าง × แท็ป) = 8192 คูณต่อหนึ่งตัวอย่าง
  2. true peak กรองทั้งไฟล์ทั้งที่พีคอยู่จุดเดียว
  3. decoder ส่งบล็อกละ ~1,150 เฟรม = **114,407 บล็อก** ทำให้ overhead
     ของการเรียก numpy กลบเวลาคำนวณจริง
- **วิธีแก้:** overlap-add FFT (688→130 วินาที) + ข้ามบล็อกที่**พิสูจน์ได้**ว่า
  ไม่มีทางมีพีค โดยใช้ขอบเขตเกนสัมบูรณ์ของ kernel (130→88) + รวมบล็อกเป็น
  65,536 เฟรมก่อนคำนวณ (88→**37 วินาที**) ค่าที่รายงานตรงกันทุกตัว
- **กันเกิดซ้ำ:** เก็บผลของเวอร์ชั่นช้าไว้เป็น**ค่าอ้างอิง** แล้วเทียบทุกครั้ง
  ที่เร่งความเร็ว — การทำให้เร็วขึ้นที่เปลี่ยนคำตอบคือการทำพัง;
  และ **float32 ไม่ได้ช่วยเรื่องความเร็วเลย** (numpy เลื่อนเป็น float64
  ข้างในอยู่ดี) วัดก่อนค่อยเชื่อ

## 50. ช่องบทปรับขนาดไม่ได้ (รายงานจากผู้ใช้)

- **อาการ:** "ช่อง text ค่อนข้างเล็ก เราดึงปรับขยายขึ้นลงได้ไหมคะ"
- **สาเหตุ:** `.transcript-panel` กำหนด `height: 30%` ตายตัว ไม่มีทางเปลี่ยน
  (`resize: vertical` ที่มีอยู่ในไฟล์เป็นของช่องแก้คำทีละคำ คนละตัว)
- **ทางที่ใช้ไม่ได้:** ใส่ `resize: vertical` ให้ตัว panel — CSS resize
  วางที่จับไว้**มุมขวาล่าง**และขยายกล่อง**ลงข้างล่าง** ซึ่งสำหรับ panel
  ที่ติดขอบล่างจออยู่แล้ว = ที่จับอยู่นอกจอ ต้องทำแถบคั่นเองเท่านั้น
- **วิธีแก้:** `panelsplit.ts` — แถบลากได้ + จำขนาดใน localStorage +
  ดับเบิลคลิกคืนค่าเดิม + ลูกศรขึ้นลงสำหรับคีย์บอร์ด
- **กันเกิดซ้ำ:** เพดานความสูง (`MIN_ABOVE_PX`) จำเป็น — ถ้าลากจนกลืนคลื่นเสียง
  หมด **ตัวแถบคั่นเองจะหลุดออกนอกจอ** แล้วลากกลับไม่ได้อีกเลย;
  และ `installPanelSplitter` ฟัง `resize` เองด้วย ส่วน `onResize`
  ก็ยิง `resize` — ต้องมีธง `notify` กันลูปไม่รู้จบ

## 51. ช่องว่างเทาๆ ใต้ spectrogram บนจอสูง (รายงานจากผู้ใช้)

- **อาการ:** "ทำไมมันมีช่องว่างด้านล่างเยอะจังคะ padding เหรอคะ"
- **สาเหตุ (ไม่ใช่ padding):** `.waves-scroll` ตั้ง `flex: 1` คือ**กินพื้นที่
  ที่เหลือทั้งหมด** แต่ลูกทั้งสองตัวมีความสูง**ตายตัว** — คลื่นเสียง 120px
  (`WAVE_HEIGHT`) และ spectrogram 190px รวม 310px พื้นที่ส่วนเกินจึงเป็น
  **ที่ว่างเปล่า** วัดได้ **244px บนจอ 1080** และยิ่งจอสูงยิ่งเยอะ
- **มีมาตั้งแต่ต้น แต่เพิ่งเห็นชัด:** ที่ความสูงราว 800px ลงมา เนื้อหา 310px
  ล้นพอดีจึงไม่มีช่องว่าง (ที่ 720px วัดได้ contentH 325 > 310 = ไม่มีช่องว่างเลย)
  ตัวที่ทำให้เจอง่ายขึ้นคือ **FIXES #50 แถบลากปรับขนาด** — พอย่อช่องบทลง
  พื้นที่ที่ได้คืนจะไหลไปกองอยู่ในโซนตายนี้ทั้งหมด
- **วิธีแก้:** ให้ spectrogram ดูดพื้นที่ส่วนเกินแทน (`flex: 1 1 auto` +
  เปลี่ยน `height: 190px` เป็น `min-height`) — และ spectrogram ที่สูงขึ้น
  **มีประโยชน์จริง** เพราะแกนความถี่เป็นลอการิทึม แถวที่เพิ่มมาจึงตกไปอยู่
  ย่านความถี่ต่ำซึ่งเป็นย่านที่ใช้หาจุดตัด
- **กันเกิดซ้ำ:** ใช้ `min-height` ไม่ใช่ `height` เพื่อให้จอเตี้ยยังเลื่อนดูได้
  (Alt+ล้อ) ตรวจแล้วทั้ง 1080px (ช่องว่าง 244→8 = padding ที่ตั้งใจไว้)
  และ 600px (spectrogram คงที่ 190 แถบเลื่อนทำงาน ปุ่มล่างไม่หลุดจอ);
  กติกา: **ถ้ากล่องแม่ตั้ง `flex: 1` ลูกต้องมีตัวที่ยืดได้อย่างน้อยหนึ่งตัว**
  ไม่งั้นส่วนเกินจะกลายเป็นที่ว่างเสมอ

## 52. แถบลากปรับขนาดช่องบท ลากได้ครั้งแรกครั้งเดียว ครั้งต่อไปขยายไม่ได้ (รายงานจากผู้ใช้)

- **อาการ:** "ตัวขยายพาเนลพอเริ่มทำงานแล้วขยายไม่ได้" — ลากครั้งแรกใช้ได้
  (หรือดูเหมือนไม่มีอะไรเกิดขึ้นเลย) แต่ลากครั้งต่อๆ ไปค้างนิ่ง เหมือนแถบตาย
- **สาเหตุ:** บั๊กที่ผมสร้างเองตอนต่อสาย `onResize` ให้ spectrogram/waveform
  รู้ว่าต้องวัดขนาดใหม่ (FIXES #50) — `onResize()` ยิง `window` "resize"
  event ซ้ำ ซึ่งไปเข้า listener ภายในของ `panelsplit.ts` เอง (ตัวที่คอย
  หด panel ให้พอดีจอตอนหน้าต่างเล็กลง) listener ตัวนั้นอ่านค่าที่**เคย
  บันทึกไว้ใน localStorage**แล้วเซ็ตทับกลับไป — แต่การบันทึกค่า
  (`storeHeight`) เกิด**หลัง**การลาก/กดปุ่มเสร็จเท่านั้น ระหว่างที่ยังลากอยู่
  (หรือกดลูกศรแต่ละครั้ง) ค่าที่บันทึกไว้จึงเป็นของ**ครั้งก่อนหน้า** เสมอ
  ผลคือทุกการขยับค่าใหม่ถูกเซ็ตทับกลับเป็นค่าเก่าทันทีในสแต็กเดียวกัน —
  ลากครั้งแรก (ยังไม่เคยบันทึกอะไรเลย) จึงดูเหมือนใช้ได้/ไม่มีอะไรเกิดขึ้น
  แต่ทุกครั้งหลังจากนั้นจะถูกดึงกลับเสมอ
- **วิธีแก้:** เปลี่ยน listener ตัวนั้นให้**อ่านค่าที่ติดอยู่บนตัว panel
  ตอนนั้นเลย** (ไม่ใช่ไปเปิด localStorage) แล้วหดให้พอดีจอถ้าเกิน — ค่าที่
  ติดอยู่บน panel ตอนนั้นคือค่าล่าสุดเสมอ (apply() เซ็ตไปแล้วก่อนยิง resize)
  จึงไม่มีทางเป็นค่าเก่าค้างอีก และเพิ่มเงื่อนไขข้ามระหว่างที่ยังลากอยู่
  (`dragging`) เพื่อไม่ให้ resize ที่ยิงเองมาแย่งอัปเดตกับการลากสด
- **กันเกิดซ้ำ:** เขียนเทสต์จำลอง DOM แบบเบา (ไม่ใช้ jsdom) ที่ต่อสาย
  `onResize` แบบเดียวกับที่ main.ts ทำจริง (ยิง `window` "resize" ซ้ำ)
  แล้วลากสองครั้งติดกันเช็คว่าค่าที่สองไม่ถูกดึงกลับไปเป็นค่าแรก — เทสต์แบบ
  เรียกฟังก์ชันตรงๆ (ไม่ผ่าน event listener จริง) จะไม่มีทางจับบั๊กนี้ได้
  เพราะบั๊กอยู่ที่**ลำดับของ event ที่ยิงซ้อนกัน** ไม่ใช่ตัวเลขคำนวณผิด

## 53. Ctrl+Z ไม่ทำงาน หลังจากมาร์กจุดหรือพิมพ์ในช่องค้นหา (รายงานจากผู้ใช้)

- **อาการ:** "ctrl+z ยังกดไม่ได้" — เกิดเป็นพักๆ ไม่ทุกครั้ง ผู้ใช้เดาว่า
  เกี่ยวกับแป้นพิมพ์ภาษาไทย (เพราะเคยเจอ FIXES #39 มาก่อน)
- **ไม่ใช่เรื่องแป้นพิมพ์ไทย:** ทดสอบยิง `e.key="ผ"` + `e.code="KeyZ"`
  (แป้นไทยจริง) → **ย้อนได้ปกติ** `isChord()` ยังทำงานถูกต้อง
- **สาเหตุจริง:** ตัว handler Ctrl+Z ตั้งใจ**ข้าม**เมื่อโฟกัสอยู่ใน
  `INPUT`/`TEXTAREA` เพราะที่นั่น Ctrl+Z ต้องเป็นของ**ข้อความ** ไม่ใช่ของ
  การตัดเสียง — ถูกต้องแล้ว **แต่กับดักคือ**: การคลิกคำในบท
  **ไม่ย้ายโฟกัส** เพราะ `<span>` ไม่ใช่ element ที่โฟกัสได้ เบราว์เซอร์จึง
  ทิ้งโฟกัสไว้ที่เดิม และ `addMarkerHere()` **โฟกัสช่องโน้ตให้อัตโนมัติ**
  (เพื่อให้พิมพ์โน้ตต่อได้ทันที) ผลคือตั้งแต่มาร์กจุดแรกเป็นต้นไป
  Ctrl+Z ทุกครั้งถูกช่องโน้ตกลืนหมด โดยไม่มีอะไรบนจอบอกเลย
  (ช่องค้นหาก็ทำให้เกิดอาการเดียวกัน)
- **ทำซ้ำได้:** มาร์กจุด → คลิกคำ → ตัด → Ctrl+Z **ไม่เกิดอะไรขึ้น**
  ตรวจ `document.activeElement` แล้วยังเป็น `INPUT.marker-note` อยู่
- **วิธีแก้:** `releaseOutsideTextFocus()` — เมื่อผู้ใช้ **mousedown ที่คำในบท
  หรือที่คลื่นเสียง** ให้ปล่อยโฟกัสจากช่องข้อความที่อยู่**นอก**พื้นที่บท
  (แปลว่า "ตอนนี้ทำงานกับเสียงแล้ว ไม่ได้พิมพ์อยู่")
- **กันเกิดซ้ำ:** ห้ามปล่อยโฟกัสของช่องแก้คำ/แก้วรรค**ในบทเอง** เพราะ
  การ blur = การ commit การแก้ (เทสต์ล็อกข้อนี้ไว้);
  และทดสอบยืนยันว่า Ctrl+Z **ตอนพิมพ์อยู่ในช่องโน้ตจริงๆ ยังเป็นของข้อความ**
  ไม่ไปย้อนการตัด — ไม่ใช่แก้ด้วยการถอดการ์ดตัวนั้นออก

## 54. ปุ่มย้อนกลับ ↺ ตายหลังใช้ปุ่มดินสอแก้ทั้งวรรค (รายงานจากผู้ใช้)

- **อาการ:** "แล้วปุ่มย้อนกลับก็กดไม่ได้เหมือนกันนะคะ" — ปุ่มเทาจนกดไม่ได้
  ทั้งที่เพิ่งตัดไปหลายจุด
- **สาเหตุ:** `replaceSegment()` (เบื้องหลังปุ่ม ✎ แก้ทั้งวรรค) สั่ง
  `this.undoStack = []` ทิ้งประวัติทั้งหมด **เป็นของที่ตั้งใจทำ** และมีเทสต์
  เขียนกำกับไว้ด้วย (`expect(p.canUndo).toBe(false)`) เหตุผลคือ snapshot
  เก็บ index ของโทเคนไว้ ซึ่ง splice ทำให้ไม่ตรงแล้ว
  **แต่ยาแรงกว่าโรค**: ผู้ใช้ตัดไปหลายจุด แก้คำผิดหนึ่งวรรค
  แล้วย้อนกลับไม่ได้อีกเลย โดยไม่มีคำอธิบาย
- **วิธีแก้:** แทนที่จะทิ้ง ให้ **remap snapshot ด้วยกติกาเดียวกับที่
  remap EDL ตัวจริง** (ช่วงที่คร่อมวรรคที่แก้ → `tokenRange = null`,
  ช่วงที่อยู่หลังวรรค → เลื่อน index ตาม delta) ทำได้เพราะ
  **เวลาของรอยตัดไม่เคยถูกแตะเลย** snapshot จึงยังบรรยายเสียงชุดเดิม
  และ `tokenRange = null` มีทางถอยอยู่แล้ว (`isTokenRemoved` เทียบเวลาแทน)
- **กันเกิดซ้ำ:** เทสต์ไม่ได้เช็คแค่ `canUndo === true` — เช็คว่า**ย้อนแล้ว
  รอยตัดที่เหลือยังชี้ที่คำของตัวเองจริง** (`effectiveText(4) === "ร้อน"`)
  เพราะถ้า remap ผิด undo จะได้ EDL ที่ index เลื่อน แล้ว .docx จะตัดคำผิดคำ
  แบบเงียบๆ — อันตรายกว่าปุ่มกดไม่ได้

## 55. ข้อความหายท้ายทุกบรรทัด ประมาณสี่ห้าคำ (รายงานจากผู้ใช้ — ร้ายแรงที่สุด)

- **อาการ:** "มันหายทุกบรรทัดค่ะ ประมาณสี่ห้าคำ เช่น ภรรยาที่มา
  แล้วก็ไปขึ้นบรรทัดใหม่เหมือนหายระหว่างทาง"
- **หลักฐานว่าหายจริง:** ใช้ไฟล์ Word ของทีม (พิมพ์จากเสียงเดียวกัน ตรงกับ
  ของเรา 96%) เป็นตัวเทียบ → **39 จาก 102 รอยต่อมีข้อความหาย** เช่น
  `'านทั้งหมด'` `'เดิน'` `'งชนะค'`; และท่อน 0 ลงท้าย**กลางคำ**ว่า
  `'เดี๋ยววั'` แล้วท่อน 1 เริ่มใหม่ว่า `'ขยายความ'` — คำว่า `นนี้อ` หายไป
- **ตัดตัวต้องสงสัยออกทีละตัว:** token กับ `segment.text` ตรงกัน**ทั้ง 103 ท่อน**
  และผลรวมทุกท่อน = `transcription.text` เป๊ะ → **ฝั่งหน้าจอกับไฟล์โปรเจกต์
  ไม่ได้ทำหาย** ปัญหาอยู่ที่ขั้นถอดเสียง
- **สาเหตุ:** `BatchedInferencePipeline` ถอดทีละก้อนแยกกันโดยอิสระ และส่ง
  `max_speech_duration_s = 30` ให้ VAD — เสียงบรรยายไทยต่อเนื่องแทบไม่มี
  ช่วงเงียบยาวพอจะจบก้อน VAD จึงชน 30 วินาทีแล้ว**ตัดตรงไหนก็ได้ = กลางคำ**
  คำที่คร่อมรอยต่อไม่เป็นของก้อนไหนเลยจึงหายไป ที่แย่กว่านั้น
  ก้อนไม่ได้ต่อกันด้วย: วัดได้ **0.32 วินาทีที่ไม่มีก้อนไหนครอบคลุมเลย**
- **วัดเทียบกับการถอดแบบเรียงลำดับ** (ผ่านเดียว ไม่มีรอยต่อ = ไม่มีอะไรให้หาย)
  บนเสียงจริง 180 วินาที:

  | วิธี | เวลา | ข้อความที่ขาด | ขาดตรงรอยต่อ |
  |---|---|---|---|
  | เดิม `vad_filter=True` | 12s | 102 ตัวอักษร | 24 |
  | **แบ่งต่อเนื่องที่จุดเงียบ ~25s** | 12s | **38** | **8** |
  | เรียงลำดับ (เกณฑ์) | 372s | — | — |

- **วิธีแก้:** `contiguous_chunks()` — กำหนดขอบเขตก้อนเอง ส่งเป็น
  `clip_timestamps` แทนที่จะให้ VAD กำหนด สองคุณสมบัติที่ขาดไม่ได้:
  1. **ต่อเนื่อง** ก้อนถัดไปเริ่มตรงที่ก้อนก่อนจบพอดี → ไม่มีเสียงตกหล่นได้เลย
  2. **ตัดตรงที่เงียบที่สุด** ในรัศมี ±3 วินาทีจากเป้า → ตกในช่วงหยุดหายใจ
     ไม่ใช่กลางคำ
- **ทำไมไม่เลิกใช้ batched ไปเลย:** เรียงลำดับช้ากว่า **31 เท่า** (372s vs 12s)
  จะทำให้ไฟล์ 50 นาทีกลับไปใช้เวลาเป็นชั่วโมงเหมือนก่อนมี GPU
- **กันเกิดซ้ำ:** เป้าตั้งไว้ 25 วินาที **ไม่ใช่ 28** เพราะ 28+3 = 31 > 30
  แล้ว faster-whisper จะ**ตัดทิ้งเหลือแค่ 30 วินาทีแรกโดยเตือนแค่ใน log**
  (ทดสอบแล้วจริง: ขาดไป 218 ตัวอักษร แย่กว่าบั๊กเดิม) เทสต์ล็อกไว้เป็น
  เลขคณิต `CHUNK_TARGET_SEC + CHUNK_SEARCH_SEC < 30` และเช็ค
  **ต่อเนื่อง/ครอบคลุมเต็ม/ไม่เกิน 30s** ทุกความยาวตั้งแต่ 26 วินาทีถึง 1 ชั่วโมง

## 56. ตัวค้นหากับตัวอักษรบนจอไม่ตรงกัน (รายงานจากผู้ใช้)

- **อาการ:** "ตัวค้นหา กับตัวอักษรไม่ตรงกันค่ะ ที่ตรึงคำ กับอักษรไม่ตรง"
- **สาเหตุ:** ปุ่ม **"ซ่อนคำที่ไม่ใช้"** ซ่อนคำที่ตัดแล้วและคำที่ขีดทิ้ง
  ด้วย CSS (`.transcript.hide-excluded { display: none }`) แต่
  `buildSearchIndex()` ยัง**นับคำพวกนั้นอยู่** ตัวค้นหาจึงทำงานกับข้อความ
  คนละชุดกับที่ตาเห็น เกิดอาการสามแบบ:
  1. พิมพ์วลีที่เห็นติดกันบนจอแล้วขึ้น **"ไม่พบ"** เพราะมีคำที่ถูกซ่อน
     คั่นอยู่ตรงกลางในดัชนี
  2. ตัวนับบอกว่าเจอ 2 ที่ แต่บนจอเห็นแค่ที่เดียว กด "ถัดไป" แล้วเหมือนไม่มีอะไรเกิดขึ้น
  3. กดไปยังผลลัพธ์แล้ว**เด้งไปที่เสียงที่ถูกตัดออกไปแล้ว** — ตรงกับ
     "ที่ตรึงคำ กับอักษรไม่ตรง"
- **ไม่ใช่ปัญหาการตรึงคำ:** ตรวจแล้วว่าการเปลี่ยนวิธีหั่นก้อนใน FIXES #55
  **ไม่ได้ทำให้การตรึงคำแย่ลง** — วัดสัดส่วนคำที่ตกลงบนความเงียบ
  (ก้อนต่อเนื่อง 4.4% vs VAD เดิม 4.1%) และความมั่นใจเฉลี่ย (0.870 vs 0.837)
  บนเสียงจริง 200 วินาที
- **วิธีแก้:** `buildSearchIndex(project, isSearchable?)` — ตัวกรองบอกว่า
  token ไหน "มองเห็นอยู่" คำที่ถูกข้ามยัง**มีตำแหน่งของตัวเองใน
  `tokenStartOffsets`** เพื่อไม่ให้ index ของ token เลื่อน (มันจะใช้ตำแหน่ง
  เดียวกับ token ตัวถัดไปที่มองเห็น และ `tokenAtOffset` คืนตัวท้ายสุดเสมอ
  จึงได้ตัวที่มองเห็น) และเรียก `runSearch()` ใหม่ทุกครั้งที่สิ่งที่มองเห็น
  เปลี่ยน — กดปุ่มซ่อน/โชว์, ตัด, ขีดทิ้งทีละคำ, ซ่อน filler ทั้งหมด
- **กันเกิดซ้ำ:** กติกา — **ตัวกรองนี้ต้องสะท้อนกฎ CSS
  `.transcript.hide-excluded` เป๊ะ** ถ้าแก้ CSS ต้องแก้ตัวกรองด้วย
  เขียนเทสต์ทั้งสองทิศทาง: ตอนซ่อนต้องหาวลีที่เห็นบนจอเจอ **และ**
  ตอนไม่ซ่อนต้องหาวลีที่ข้ามคำไม่เจอ (ไม่งั้นแก้จนหลวมเกินไปโดยไม่รู้ตัว)

## เช็คลิสต์สุขภาพระบบ (เมื่อ "ปุ่มกดไม่ได้/ไม่มีอะไรเกิดขึ้น")

1. Status bar ล่างซ้ายเขียวไหม? แดง = backend ไม่รัน →
   `cd backend && .venv/bin/uvicorn app.main:app --reload`
2. ปุ่มถอดเสียงต้องการ: เปิดไฟล์แล้ว + backend เขียว
3. ปุ่มบันทึก/ซ่อนfiller/ค้นหา ต้องการ: มี transcript แล้ว (ถอดเสร็จหรือเปิดโปรเจกต์)
4. แก้โค้ด frontend แล้วแอปรีโหลดเอง → ไฟล์/โปรเจกต์ที่เปิดอยู่หาย ต้องเปิดใหม่
